---
phase: 06-ux-visual-expansion
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - ui_service/src/lib/adminClient.ts
  - ui_service/src/app/api/admin/[...path]/route.ts
  - ui_service/src/lib/errors.ts
  - ui_service/src/machines/nexusApp.ts
  - ui_service/src/app/dashboard/page.tsx
  - ui_service/src/components/ui/error-states.tsx
  - ui_service/src/lib/runtime-params.ts
  - shared/contracts/ui_capability_schema.py
  - orchestrator/admin_api.py
  - tests/unit/test_capability_contract.py
  - tests/unit/ui/test_nexus_app_machine.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dashboard never hard-fails to blank/locked-only view when admin auth is missing; it renders explicit degraded state with actionable reason"
    - "All browser admin reads use same-origin BFF path, never direct cross-origin calls to port 8003"
    - "Capability lock state and visibility are sourced from one contract model (single source of truth), not inferred independently by page components"
    - "Fallback payload is contract-valid and reason-coded (e.g., admin_auth_required), not silent empty arrays without context"
    - "XState capability region distinguishes auth/degraded/network states and maps each to deterministic UI states"
    - "Monitoring latency endpoint exists and returns contract-stable shape in all environments"
    - "Polling/timeout/retry intervals are centralized in one runtime parameter module shared by machines"
    - "UI and backend configuration parameters remain synchronized through typed contract fields and tests"
  artifacts:
    - path: "ui_service/src/lib/runtime-params.ts"
      provides: "Single source runtime parameters for polling, timeout, retry, fallback behavior"
      min_lines: 30
    - path: "ui_service/src/lib/adminClient.ts"
      provides: "Same-origin admin access and deterministic error mapping for capability/config/version reads"
      exports: ["adminApi"]
      min_lines: 120
    - path: "ui_service/src/app/api/admin/[...path]/route.ts"
      provides: "BFF proxy with controlled fallback semantics and reason-coded responses"
      exports: ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
      min_lines: 80
    - path: "ui_service/src/machines/nexusApp.ts"
      provides: "XState capability state model with explicit auth/degraded/network handling"
      exports: ["nexusAppMachine"]
      min_lines: 180
    - path: "ui_service/src/app/dashboard/page.tsx"
      provides: "Deterministic rendering policy for degraded/locked/empty states without silent collapse"
      min_lines: 220
    - path: "shared/contracts/ui_capability_schema.py"
      provides: "Capability contract extensions for source/health/reason metadata used by UI state model"
      exports: ["CapabilityEnvelope"]
      min_lines: 150
  key_links:
    - from: "ui_service/src/lib/adminClient.ts"
      to: "ui_service/src/app/api/admin/[...path]/route.ts"
      via: "same-origin fetch path for all admin reads"
      pattern: "\/api\/admin"
    - from: "ui_service/src/machines/nexusApp.ts"
      to: "ui_service/src/lib/errors.ts"
      via: "classifyError drives deterministic capability transitions"
      pattern: "classifyError|NexusErrorType"
    - from: "ui_service/src/app/dashboard/page.tsx"
      to: "ui_service/src/components/ui/error-states.tsx"
      via: "degraded/auth/network fallback rendering"
      pattern: "DegradedBanner|EmptyState|TimeoutSkeleton"
    - from: "orchestrator/admin_api.py"
      to: "shared/contracts/ui_capability_schema.py"
      via: "envelope includes reason-coded feature status metadata"
      pattern: "CapabilityEnvelope"
---

<objective>
Harden Phase 06 runtime stability so dashboard visibility/lock state remains reliable under auth or network faults, while preserving a single contract-driven source of truth and SOLID boundaries.

Purpose: Recent failures showed that cross-origin auth rejection and implicit empty fallback can collapse UI semantics into "locked" or blank behavior. This plan converts those failure paths into explicit, contract-valid, deterministic states with centralized parameters and synchronized backend/frontend configuration.

Output: Single-source runtime parameters, deterministic capability error mapping, reason-coded fallback contract, and dashboard rendering policy that never silently degrades.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-minimal.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-visual-expansion/06-CONTEXT.md
@.planning/phases/06-ux-visual-expansion/VERIFICATION.md
@.planning/phases/06-ux-visual-expansion/06-04-SUMMARY.md
@NEXUS Phase 5B Microservice Hardening Plan.md

Scope alignment notes:
- Keep UX simple and deterministic: no new pages or visual complexity.
- Preserve capability-driven architecture; remove split-brain lock inference.
- Integrate infrastructure constraints from Phase 5B (health protocol, overhead budgets) only where they affect Phase 06 runtime stability.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate admin read path and runtime parameters into single-source configuration</name>
  <files>ui_service/src/lib/runtime-params.ts, ui_service/src/lib/adminClient.ts, ui_service/src/app/api/admin/[...path]/route.ts</files>
  <action>
    Create `ui_service/src/lib/runtime-params.ts` as canonical runtime parameter source for:
    - capability poll interval, config poll interval, request timeout, retry delays, fallback mode switches.
    - ensure machines and client read values from this module only.

    Refactor admin client + proxy path so all browser reads go through same-origin BFF consistently:
    - prohibit direct browser calls to `localhost:8003`.
    - preserve server-side access to internal admin URL where needed.

    Tighten proxy fallback semantics:
    - only return fallback for explicitly approved read-only endpoints.
    - include reason metadata (`admin_auth_required`, `upstream_unavailable`) in fallback responses.
    - do not mutate or broaden auth scope.

    Keep logic simple: no extra abstraction layers beyond one parameter module and existing admin client/proxy boundary.
  </action>
  <verify>
    `grep -R "localhost:8003/admin" ui_service/src --include="*.ts" --include="*.tsx"` returns no browser-side direct admin calls.
    `grep -R "runtime-params" ui_service/src/machines ui_service/src/lib --include="*.ts"` shows machines/client consume shared params.
    `curl -s http://localhost:5001/api/admin/admin/capabilities | jq '.config_version,.fallback_reason?'` returns contract-valid JSON.
  </verify>
  <done>
    Admin read flow is same-origin and centralized. Runtime constants are no longer scattered. Fallback payloads are explicit and diagnosable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make lock/degraded/empty rendering deterministic from contract + XState</name>
  <files>shared/contracts/ui_capability_schema.py, orchestrator/admin_api.py, ui_service/src/lib/errors.ts, ui_service/src/machines/nexusApp.ts, ui_service/src/app/dashboard/page.tsx, ui_service/src/components/ui/error-states.tsx</files>
  <action>
    Extend capability contract minimally to carry explicit reason/source metadata needed by UI state logic.

    Refine error taxonomy and machine transitions so capability region distinguishes:
    - auth-required state,
    - degraded upstream state,
    - transport/network failure,
    - valid-empty capability set.

    Update dashboard render policy:
    - locked adapters render with reason and recovery guidance.
    - degraded contract renders DegradedBanner (not silent empty grid).
    - valid empty set renders EmptyState intentionally.
    - remove implicit inference that treats every missing/empty value as lock.

    Maintain SOLID boundaries:
    - backend defines capability truth,
    - machine interprets state transitions,
    - page component only renders declared state.
  </action>
  <verify>
    `python -m pytest tests/unit/test_capability_contract.py -v` passes.
    `grep -n "AUTH\|DEGRADED\|NETWORK\|EMPTY" ui_service/src/machines/nexusApp.ts` confirms explicit branches.
    Manual check: dashboard never shows generic "Failed to fetch" for auth fallback; shows reason-coded degraded/auth state.
  </verify>
  <done>
    UI lock/degraded behavior is contract-driven and deterministic, with no silent collapse into unusable dashboard state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add synchronization guard tests for frontend/backend parametric drift</name>
  <files>tests/unit/ui/test_nexus_app_machine.ts, tests/unit/test_capability_contract.py</files>
  <action>
    Add focused tests that fail on sync drift:
    - contract fallback still satisfies CapabilityEnvelope shape.
    - machine maps reason-coded fallback to expected state.
    - runtime polling constants are read from shared params module.
    - monitoring latency route always returns `{latency, agentRuns}` shape.

    Keep test scope narrow and stable (no broad E2E additions). These are guardrails for regression on the exact failure mode observed.
  </action>
  <verify>
    `python -m pytest tests/unit/test_capability_contract.py -v` passes.
    `cd ui_service && npm test -- test_nexus_app_machine` (or project-equivalent unit test command) passes.
  </verify>
  <done>
    Parametric synchronization is protected by regression tests that catch contract/render drift before dashboard breakage reaches runtime.
  </done>
</task>

</tasks>

<verification>
- Reproduce prior scenario: missing admin key + dashboard open.
- Confirm dashboard renders deterministic degraded/auth states, not fatal fetch failure.
- Confirm monitoring latency route responds and no 404 appears in browser console.
- Confirm capability poll loop remains stable (no error spam loops).
</verification>

<success_criteria>
- Dashboard remains usable under auth/network fault paths with explicit state messaging.
- Lock visibility and adapter card rendering are synchronized to one contract truth.
- Runtime parameters are centralized and shared across machine/client layers.
- No direct browser cross-origin admin calls remain in UI code.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-visual-expansion/06-05-SUMMARY.md`
</output>
