---
phase: 07-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/audit/__init__.py
  - shared/audit/store.py
  - shared/audit/redaction.py
  - shared/audit/context.py
  - tests/unit/test_audit_store.py
  - Makefile
autonomous: true
requirements: ["REQ-010"]
must_haves:
  truths:
    - "audit_events rows can never be updated or deleted — SQLite triggers RAISE(ABORT) on both (D-02)"
    - "Every row carries prev_hash/row_hash forming a verifiable hash chain from a genesis constant (D-02)"
    - "AuditStore.record() raises AuditWriteError on any write failure — no silent swallowing (D-03)"
    - "Secret values in before/after/details are redacted at the store layer unconditionally — callers cannot opt out (D-04)"
    - "Two concurrent writer processes produce contiguous ids and a valid chain (BEGIN IMMEDIATE serialization)"
    - "A Phase-8-style approval event (details with bundle_sha256) round-trips without schema change (D-06)"
  artifacts:
    - path: "shared/audit/store.py"
      provides: "AuditStore with record/query/count/iter_events/verify_chain, get_audit_store/set_audit_store singleton"
      contains: "class AuditStore"
    - path: "shared/audit/redaction.py"
      provides: "deep redact() with SHA-256 fingerprints for secret-keyed values"
      contains: "def redact"
    - path: "shared/audit/context.py"
      provides: "ActorContext, contextvar accessors, actor_context(), AuditContextMiddleware"
      contains: "class AuditContextMiddleware"
    - path: "tests/unit/test_audit_store.py"
      provides: "unit coverage for triggers, chain, redaction, fail-closed, concurrency"
  key_links:
    - from: "shared/audit/store.py record()"
      to: "shared/audit/redaction.py"
      via: "unconditional redact() call before serialization"
      pattern: "redact\\("
    - from: "shared/audit/context.py AuditContextMiddleware"
      to: "request.state.user set by shared/auth/middleware.py"
      via: "reads request.state.user/org_id after auth middleware runs"
      pattern: "request\\.state\\.user"
---

<objective>
Deliver the audit primitive for the whole platform: an append-only, hash-chained, tamper-evident SQLite `audit_events` store with store-layer secret redaction, fail-closed write semantics, and an ambient actor-context mechanism. This is REQ-010 plus the foundations every later capture path (decorator, dual-write, chat tools) plugs into.

Output: `shared/audit/` package (store, redaction, context, exports), unit test suite, `audit-test` Makefile target.
</objective>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-audit-trail/07-CONTEXT.md

<interfaces>
<!-- Verified against the codebase; do not re-explore. -->

shared/billing/usage_store.py — THE pattern template: UsageStore(:17), db_path default (:20),
`PRAGMA journal_mode=WAL` in connection helper (:28), _init_db (:32) with CREATE TABLE IF NOT EXISTS
+ two CREATE INDEX IF NOT EXISTS (:51,:55). Mirror: Path(db_path), parent.mkdir(parents=True, exist_ok=True),
connection-per-call, busy_timeout=10000 (shared/auth/api_keys.py:35-39 shows busy_timeout usage),
conn.row_factory = sqlite3.Row set in _connect (shared/auth/user_prefs.py:70-76 shows this variant — prefer it).

shared/auth/middleware.py:73-74 — APIKeyAuthMiddleware attaches `request.state.user` (shared.auth.models.User
with .user_id, .org_id, .role) and `request.state.org_id`. AuditContextMiddleware reads these; tolerate absence
(public paths). IP: first hop of X-Forwarded-For header, else request.client.host.

Timestamp convention: use `datetime.now(timezone.utc).isoformat()` (matches user_prefs.py; do NOT append "Z"
on top of an offset-aware isoformat).

Canonical serialization for hashing: json.dumps(payload, sort_keys=True, separators=(",", ":")).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared/audit package — store, redaction, context</name>
  <files>shared/audit/__init__.py, shared/audit/store.py, shared/audit/redaction.py, shared/audit/context.py</files>
  <read_first>
    - shared/billing/usage_store.py (full — the store pattern to mirror)
    - shared/auth/user_prefs.py (lines 60-95 — _connect with row_factory, WAL)
    - shared/auth/middleware.py (full, 96 lines — request.state contract, BaseHTTPMiddleware shape)
  </read_first>
  <behavior>
    - AuditStore(db_path) creates the DB (parents mkdir), table, 4 indexes, and 2 append-only triggers idempotently
    - record(org_id, actor_id, action, resource_type, resource_id=None, before_state=None, after_state=None, ip_address=None, details=None) -> int: assigns id explicitly inside a BEGIN IMMEDIATE transaction, computes prev_hash (previous row_hash or GENESIS_HASH="0"*64) and row_hash = sha256(canonical_json of all fields incl. id and prev_hash), redacts all three dict payloads unconditionally, inserts, returns id; ANY exception → raise AuditWriteError(...) from e
    - UPDATE/DELETE against audit_events raise sqlite3.DatabaseError via triggers
    - query(**filters, limit=100, offset=0) ORDER BY id DESC; count(**filters); iter_events(batch_size=1000, **filters) ascending-id cursor pagination
    - verify_chain(start_id=None, end_id=None) -> ChainVerificationResult(valid, checked, first_invalid_id, reason): recomputes each row_hash, checks prev_hash linkage + id contiguity
    - redact(): dict keys matching (?i)(api[_-]?key|secret|token|password|credential|authorization|private[_-]?key) → string values become "[REDACTED sha256:<first-16-hex>]", non-strings "[REDACTED]"; recurses dicts/lists; non-JSON-serializable leaf → str()
    - context.py: frozen ActorContext(actor_id, org_id, ip_address=None, channel="http"); set_actor/reset_actor/get_actor; actor_context() contextmanager; AuditContextMiddleware sets ActorContext from request.state for the request scope and resets after
    - get_audit_store() lazy singleton from os.getenv("AUDIT_DB_PATH", "data/audit_events.db"); set_audit_store() override for servers/tests
  </behavior>
  <action>
    Create the four files per the behavior contract above, mirroring the UsageStore SQLite conventions. DDL columns: id INTEGER PRIMARY KEY, timestamp TEXT NOT NULL, org_id TEXT NOT NULL, actor_id TEXT NOT NULL, action TEXT NOT NULL, resource_type TEXT NOT NULL, resource_id TEXT, before_state TEXT, after_state TEXT, ip_address TEXT, details TEXT, prev_hash TEXT NOT NULL, row_hash TEXT NOT NULL. Indexes: (org_id,timestamp), (actor_id,timestamp), (action,timestamp), (resource_type,resource_id). Triggers audit_events_no_update / audit_events_no_delete BEFORE UPDATE/DELETE → SELECT RAISE(ABORT, 'audit_events is append-only'). record() must open its own connection with isolation_level=None and run BEGIN IMMEDIATE → SELECT last (id,row_hash) → compute → INSERT (all columns explicit) → COMMIT. __init__.py re-exports AuditStore, AuditWriteError, ChainVerificationResult, get_audit_store, set_audit_store, ActorContext, set_actor, reset_actor, get_actor, actor_context, AuditContextMiddleware, redact, fingerprint. Do NOT use `from __future__ import annotations` anywhere near Pydantic imports (project memory) — these modules need no Pydantic at all; keep them dependency-light (stdlib + starlette for the middleware only).
  </action>
  <verify>
    <automated>python -c "from shared.audit import AuditStore, AuditContextMiddleware, redact, get_audit_store; import tempfile, os; s=AuditStore(os.path.join(tempfile.mkdtemp(),'a.db')); i=s.record(org_id='o',actor_id='a',action='t',resource_type='r',details={'api_key':'sk-123'}); assert i==1; assert s.verify_chain().valid"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "RAISE(ABORT" shared/audit/store.py returns 2
    - grep -n "BEGIN IMMEDIATE" shared/audit/store.py returns a match
    - python import check above exits 0 and the recorded row's details contain "[REDACTED" not "sk-123"
  </acceptance_criteria>
  <done>shared/audit package importable; append-only + chained + redacting store operational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test suite + Makefile target</name>
  <files>tests/unit/test_audit_store.py, Makefile</files>
  <read_first>
    - tests/unit/test_billing.py (lines 1-60 — tmp_path fixture style for SQLite stores)
    - Makefile (lines 775-795 — auth-test/billing-test target shape; line ~54 .PHONY list)
  </read_first>
  <behavior>
    - Trigger enforcement: raw sqlite3 UPDATE and DELETE against audit_events raise
    - Chain: N inserts → verify_chain().valid, checked==N; genesis prev_hash == "0"*64 on row 1
    - Tamper detection: drop triggers via raw connection, mutate one row's action, recreate triggers → verify_chain() reports valid=False with the exact first_invalid_id
    - Redaction: nested dict/list secrets redacted with stable fingerprints; plain keys untouched; fail-closed: record() to a read-only db file raises AuditWriteError
    - Concurrency: 2 processes (multiprocessing) × 50 record() each against the same db → 100 rows, contiguous ids 1..100, valid chain
    - Context: set/get/reset roundtrip; propagation into anyio.to_thread / threadpool (contextvars copied)
    - D-06 shape: record(action="module_approved", resource_type="module", details={"bundle_sha256": "abc", "timestamp": "..."}) round-trips via query()
  </behavior>
  <action>
    Write tests/unit/test_audit_store.py covering every behavior above using tmp_path fixtures (never data/). For the multiprocess test use a module-level worker function (picklable) that constructs its own AuditStore on the shared tmp db path and retries on AuditWriteError caused by busy contention up to a bounded count. Add `audit-test` to the Makefile .PHONY list and a target after auth-test using @printf (NOT @echo — macOS ANSI, project memory): `cd tests && python -m pytest unit/test_audit_store.py -v --tb=short`.
  </action>
  <verify>
    <automated>cd tests && python -m pytest unit/test_audit_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - make audit-test runs and passes
    - Tamper test asserts first_invalid_id identifies the mutated row
    - Concurrency test asserts 100 contiguous ids and verify_chain().valid
  </acceptance_criteria>
  <done>make audit-test green; store behavior locked by tests.</done>
</task>

</tasks>

<verification>
- `make audit-test` green
- `python -c "from shared.audit import AuditStore"` exits 0
</verification>

<success_criteria>
- Append-only enforced by triggers, tamper-evident via hash chain, fail-closed writes, unconditional redaction (REQ-010, D-02/D-03/D-04)
- Actor-context mechanism ready for Plan 07-02 wiring; D-06 event shape verified
</success_criteria>

<output>
Create `.planning/phases/07-audit-trail/07-01-SUMMARY.md` when done.
</output>
