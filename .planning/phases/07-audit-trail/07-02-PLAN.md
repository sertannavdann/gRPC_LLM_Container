---
phase: 07-audit-trail
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - shared/audit/decorator.py
  - shared/audit/__init__.py
  - shared/modules/audit.py
  - orchestrator/admin_api.py
  - orchestrator/orchestrator_service.py
  - core/graph.py
  - tools/builtin/module_admin.py
  - tools/builtin/module_pipeline.py
  - dashboard_service/main.py
  - tests/unit/test_audit_decorator.py
  - tests/integration/admin/test_audit_capture.py
  - tests/integration/admin/conftest.py
  - tests/integration/cross_feature/test_audit_completeness.py
autonomous: true
requirements: ["REQ-011"]
must_haves:
  truths:
    - "Every orchestrator Admin API mutation endpoint records an audit event with actor from the authenticated user (REQ-011)"
    - "A failed audit write rejects the mutation with HTTP 500 (D-03)"
    - "DevModeAuditLog dual-writes: JSONL line AND SQLite row for every draft/version action; sink failure raises (D-01)"
    - "Chat-tool mutations (enable/disable/credentials/uninstall/install) record audit events with actor from graph state via contextvar (D-05)"
    - "Dashboard admin mutations authenticate an actor (Depends(get_current_user)) and record audit events (D-05)"
    - "No plaintext secret ever lands in the audit DB — credential events carry key names only, store redaction as backstop (D-04)"
  artifacts:
    - path: "shared/audit/decorator.py"
      provides: "@audit_action(action, resource_type, resource_id=template, snapshot=callable) sync+async"
      contains: "def audit_action"
    - path: "orchestrator/admin_api.py"
      provides: "decorated mutation endpoints + audit_store param on start_admin_server + AuditContextMiddleware"
    - path: "dashboard_service/main.py"
      provides: "authenticated + decorated admin mutations"
    - path: "core/graph.py"
      provides: "actor_context wrapping tool execution in _tools_node"
      contains: "actor_context"
  key_links:
    - from: "orchestrator/admin_api.py start_admin_server"
      to: "shared/audit/store.py set_audit_store"
      via: "singleton wiring at server start"
      pattern: "set_audit_store"
    - from: "shared/modules/audit.py DevModeAuditLog.log_action"
      to: "shared/audit/store.py AuditStore.record"
      via: "optional sink dual-write"
      pattern: "self\\.sink\\.record|self\\._sink\\.record"
    - from: "tools/builtin/module_admin.py mutating strategies"
      to: "shared/audit/context.py get_actor"
      via: "ambient actor resolution"
      pattern: "get_actor"
---

<objective>
Wire the audit primitive into every mutation path: the `@audit_action` decorator over orchestrator and dashboard HTTP mutations, dual-write from `DevModeAuditLog` (draft/version lifecycle), and direct `record()` calls in chat-tool strategies with contextvar-delivered actor identity. After this plan, no mutation path can bypass the audit trail (REQ-011, D-01/D-03/D-05).
</objective>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-audit-trail/07-CONTEXT.md

<interfaces>
<!-- Verified against the codebase; do not re-explore beyond confirming line drift. -->

orchestrator/admin_api.py — module-global `_app = FastAPI(...)` (:40); collaborators are module globals set by
start_admin_server (:1707-1719); APIKeyAuthMiddleware added inside start_admin_server (:1745-1755). Handlers are
sync `def`. RBAC: `user: User = Depends(require_permission(Permission.X))`. CAUTION: parameter named `request` is
sometimes the Pydantic body and the Starlette Request is `http_request` (store_credentials :413); some handlers
have NO Request param (enable_module :233) — the decorator must find Request instances by type, tolerating absence.
Mutations to decorate: routing-config PUT :108 / PATCH :118 / DELETE :131 / reload :145; module enable :233 /
disable :258 / reload :283 / uninstall (DELETE) :381; credentials POST :413 / DELETE :445; /admin/reload :532;
user prefs PUT :1164; api-keys POST :1256 / DELETE :1283 / rotate :1297. Draft/version endpoints :575-707 get NO
decorator (covered via DevModeAuditLog sink); the block at :1383-1543 is DEAD (registration order) — mirror the
actor= fix there for consistency but add nothing else. Bootstrap :1233 is public: after key creation, call
_audit_store.record(org_id=<new org>, actor_id="bootstrap", action="bootstrap_completed", resource_type="api_key",
after_state={"key_id":..., "role":"owner"}). Existing DevModeAuditLog calls pass actor=user.org_id — change to
user.user_id (org_id goes in the org_id field now).
NOTE: Phase 8 (merged before this plan executes) added approve/reject endpoints near the active draft block —
leave them undecorated in this plan (approval.py already logs via DevModeAuditLog → sink covers them); do not
double-record.

Middleware ordering: Starlette add_middleware = last-added is OUTERMOST. AuditContextMiddleware must run INNER
(after auth sets request.state.user) → add it BEFORE the APIKeyAuthMiddleware add_middleware call in code order.

orchestrator/orchestrator_service.py — TWO DevModeAuditLog instances: ~:979 (tools path, into DraftManager :980 /
VersionManager :985) and ~:1816 (admin API path, into start_admin_server :1827+). Both need sink=<AuditStore>.
Construct one shared AuditStore from os.getenv("AUDIT_DB_PATH", "data/audit_events.db") and pass it to both plus
audit_store= on start_admin_server and into ModuleAdminTool/ModulePipelineTool constructors (~:999-1046).

core/graph.py — _tools_node :383; tool call `tool(**tool_args)` ~:427; state keys org_id :478 (`state.get("org_id") or "default"`),
user_id :495. Wrap the tool invocation in actor_context(ActorContext(actor_id=state.get("user_id") or "chat_agent",
org_id=state.get("org_id") or "default", channel="chat")).

tools/builtin/module_admin.py — ModuleAdminTool(CompositeTool) :251, strategies registered :275-286. Mutating +
currently unaudited: EnableStrategy :58, DisableStrategy :80, CredentialStrategy :100 (kwargs api_key /
credentials_json — NEVER put values in the event; details={"credential_keys": sorted(creds)}), UninstallStrategy :137.
Draft strategies :150-247 log via DevModeAuditLog with hardcoded actor="chat_agent" — replace with
(get_actor().actor_id if get_actor() else "chat_agent"). tools/builtin/module_pipeline.py — InstallStrategy :70
records action="module_installed" on success. Audit failure in a strategy → return {"status":"error","error":"audit write failed"}.

dashboard_service/main.py — app :321; APIKeyAuthMiddleware added :330-345; the four admin mutations have NO auth
dependency today: POST /admin/module-credentials/{category}/{platform} :812, DELETE :824, POST /admin/credentials :844
(actor from user_id query param!), POST /admin/disconnect :890. Add `user: User = Depends(get_current_user)`
(imports from shared.auth) + decorate. /admin/credentials and /admin/disconnect mutate os.environ + cache:
resource_type="runtime_credentials", snapshots = {env_key: bool(os.environ.get(env_key))} presence booleans only.
Handlers here are async def — decorator must support both.

shared/modules/audit.py — DevModeAuditLog :346, __init__ :362 (audit_dir), log_action :373-414 (appends JSONL).
Add sink param; after JSONL append: sink.record(org_id=ctx.org_id if ctx else "default", actor_id=ctx.actor_id if ctx
else actor, action=action, resource_type="module_draft" if draft_id else "module", resource_id=draft_id or module_id,
details={**(details or {}), "module_id": module_id, "draft_id": draft_id, "jsonl_event_id": event_id},
ip_address=ctx.ip_address if ctx else None). NO try/except around sink.record (D-03).

tests/integration/admin/conftest.py — create_test_admin_app() :32 builds a minimal app (does NOT import admin_api);
tmp_databases fixture :271-317. Add an "audit" tmp db + audit_store fixture, set_audit_store() wiring, add
AuditContextMiddleware before the auth middleware add (:375 area), and add representative decorated endpoints
(one config, one module, one credential mutation) to the test app.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @audit_action decorator + unit tests</name>
  <files>shared/audit/decorator.py, shared/audit/__init__.py, tests/unit/test_audit_decorator.py</files>
  <read_first>
    - shared/audit/store.py, shared/audit/context.py (from 07-01 — the primitives)
    - tools/decorators.py (functools.wraps decorator template style)
  </read_first>
  <behavior>
    - audit_action(action, resource_type, resource_id=None (str template over bound args, e.g. "{category}/{platform}"), snapshot=None, store=None) wraps sync and async callables; functools.wraps preserves __wrapped__ so FastAPI sees the original signature
    - before = snapshot(**matching bound args) pre-call; after = snapshot(...) post-call, else handler result if dict, else None; snapshot exceptions → None + logger.warning, event still recorded
    - Handler exceptions propagate WITHOUT recording (failed mutations unaudited in this phase)
    - Actor resolution: get_actor() contextvar → scan bound args for starlette Request instance (request.state.user + client IP) → fallback ActorContext("system","default")
    - AuditWriteError from store.record → fastapi.HTTPException(500, "Audit write failed; mutation not recorded — the state change may have applied")
    - store defaults to get_audit_store() at call time (not import time)
  </behavior>
  <action>
    Implement the decorator per the behavior contract. Unit tests (tmp_path store, no FastAPI server needed for most): sync fn capture, async fn capture, resource_id templating, snapshot before/after, snapshot exception tolerance, handler exception → no event, store failure → HTTPException(500), no-Request-param + no-contextvar → actor "system", contextvar wins over Request scan. Export audit_action from shared/audit/__init__.py.
  </action>
  <verify>
    <automated>cd tests && python -m pytest unit/test_audit_decorator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Decorated async and sync handlers both produce audit rows with correct action/resource_type/resource_id
    - Store-failure test asserts HTTPException with status 500
  </acceptance_criteria>
  <done>Decorator behavior locked by unit tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire all capture paths (orchestrator, dashboard, tools, graph, dual-write)</name>
  <files>shared/modules/audit.py, orchestrator/admin_api.py, orchestrator/orchestrator_service.py, core/graph.py, tools/builtin/module_admin.py, tools/builtin/module_pipeline.py, dashboard_service/main.py</files>
  <read_first>
    - orchestrator/admin_api.py (verify line drift from the interface notes — Phase 8 merge added approve/reject endpoints)
    - shared/modules/audit.py (:346-414)
    - core/graph.py (:383-510)
    - tools/builtin/module_admin.py (:20-290), tools/builtin/module_pipeline.py (:60-130)
    - dashboard_service/main.py (:320-360, :798-920)
  </read_first>
  <behavior>
    - All interface-note mutations decorated; draft/version endpoints and Phase 8 approve/reject NOT decorated (sink covers them)
    - start_admin_server accepts audit_store=, sets module global + set_audit_store(), adds AuditContextMiddleware before APIKeyAuthMiddleware
    - Both DevModeAuditLog instances constructed with sink=; shared AuditStore instance threaded through orchestrator_service
    - _tools_node wraps tool execution in actor_context(channel="chat")
    - ModuleAdminTool/ModulePipelineTool accept audit_store=; mutating strategies record on success, fail closed on AuditWriteError
    - Dashboard mutations authenticated (get_current_user) + decorated; AuditContextMiddleware added inner of auth
    - actor= at DevModeAuditLog call sites in admin_api switched from user.org_id to user.user_id (live block; mirror dead block)
  </behavior>
  <action>
    Apply the wiring exactly as specified in the interface notes. Keep every admin_api.py change additive (decorator lines above handlers, appended start_admin_server param) — Phase 8's approval endpoints are freshly merged; do not restructure their block. Snapshot lambdas close over module globals and must be None-tolerant (e.g. registry not initialized). For api-key endpoints, after_state carries {key_id, role} only.
  </action>
  <verify>
    <automated>python -c "import orchestrator.admin_api, dashboard_service.main, core.graph, tools.builtin.module_admin, tools.builtin.module_pipeline, shared.modules.audit" && cd tests && python -m pytest unit/test_audit_store.py unit/test_audit_decorator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@audit_action" orchestrator/admin_api.py >= 14
    - grep -n "AuditContextMiddleware" orchestrator/admin_api.py dashboard_service/main.py both match, added before the auth middleware add in code order
    - grep -n "sink" shared/modules/audit.py shows the dual-write with no try/except around it
    - grep -n "Depends(get_current_user)" dashboard_service/main.py shows 4 matches on the admin mutations
    - grep -n "actor_context" core/graph.py matches inside _tools_node
  </acceptance_criteria>
  <done>Every mutation path feeds the audit store; imports clean; unit suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — capture parity across paths</name>
  <files>tests/integration/admin/test_audit_capture.py, tests/integration/admin/conftest.py, tests/integration/cross_feature/test_audit_completeness.py</files>
  <read_first>
    - tests/integration/admin/conftest.py (full — create_test_admin_app factory, tmp_databases, per-role key fixtures)
    - tests/integration/admin/test_module_crud.py (endpoint-call + RBAC assertion pattern)
    - tests/integration/cross_feature/test_audit_completeness.py (existing draft-lifecycle audit assertions)
  </read_first>
  <behavior>
    - TestClient against the extended test app: a decorated mutation records actor_id == the calling key's user, org scoping correct, ip present via middleware
    - Monkeypatched store.record raising AuditWriteError → endpoint returns 500 and the event count is unchanged
    - DevModeAuditLog with sink: one log_action → JSONL line AND SQLite row (jsonl_event_id cross-reference); sink failure propagates
    - ModuleAdminTool EnableStrategy under actor_context(channel="chat") records that actor; without context falls back to chat_agent
    - Credential mutation events contain "[REDACTED" or key names only — assert raw value absent from the DB file bytes
    - test_audit_completeness extended: every draft/version lifecycle action asserts SQLite parity, not just JSONL
  </behavior>
  <action>
    Extend conftest with the audit fixtures + middleware + three representative decorated endpoints per the interface notes. Write test_audit_capture.py covering the behaviors. Extend test_audit_completeness.py with a SQLite-parity assertion helper.
  </action>
  <verify>
    <automated>cd tests && python -m pytest integration/admin/test_audit_capture.py integration/cross_feature/test_audit_completeness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Capture, fail-closed, dual-write-parity, chat-actor, and redaction tests all green
    - Existing admin integration suite still green: cd tests && python -m pytest integration/admin/ -q
  </acceptance_criteria>
  <done>Cross-path capture parity proven; no regressions in the admin suite.</done>
</task>

</tasks>

<verification>
- `make audit-test` (unit files) + `cd tests && python -m pytest integration/admin/ -q` green
- `make auth-test` green (middleware ordering / RBAC regression)
</verification>

<success_criteria>
- REQ-011 delivered: decorator + dual-write + chat-tool capture; fail-closed everywhere; no plaintext secrets in the DB
</success_criteria>

<output>
Create `.planning/phases/07-audit-trail/07-02-SUMMARY.md` when done.
</output>
