---
phase: 07-audit-trail
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - shared/auth/rbac.py
  - orchestrator/admin_api.py
  - shared/observability/metrics.py
  - shared/audit/store.py
  - config/grafana/provisioning/dashboards/json/nexus-modules.json
  - docker-compose.yaml
  - Makefile
  - tests/integration/admin/test_audit_query_api.py
  - tests/integration/admin/conftest.py
autonomous: true
requirements: ["REQ-012"]
must_haves:
  truths:
    - "GET /admin/audit-logs filters by date range, actor, action, resource and paginates; org-scoped to the caller unless OWNER (REQ-012)"
    - "GET /admin/audit-logs/export streams CSV with an injection guard and attachment headers (REQ-012)"
    - "GET /admin/audit-logs/verify runs the chain check and reports the first invalid row (D-02)"
    - "Audit read access requires the new READ_AUDIT permission — viewer/operator 403, admin/owner 200"
    - "nexus_audit_events_total / nexus_audit_write_failures_total exported; Grafana Audit Trail row renders"
    - "Both containers resolve AUDIT_DB_PATH to the same host file ./data/audit_events.db"
  artifacts:
    - path: "shared/auth/rbac.py"
      provides: "Permission.READ_AUDIT granted to ADMIN (OWNER automatic)"
      contains: "READ_AUDIT"
    - path: "orchestrator/admin_api.py"
      provides: "audit-logs query + export + verify endpoints"
      contains: "/admin/audit-logs"
    - path: "config/grafana/provisioning/dashboards/json/nexus-modules.json"
      provides: "Audit Trail row, panel ids 17-20"
  key_links:
    - from: "orchestrator/admin_api.py audit endpoints"
      to: "shared/audit/store.py query/iter_events/verify_chain"
      via: "module-global _audit_store"
      pattern: "_audit_store\\.(query|iter_events|verify_chain)"
    - from: "shared/audit/store.py record()"
      to: "shared/observability/metrics.py AuditMetrics"
      via: "lazy metrics increment (success + failure paths)"
      pattern: "audit_events_total|audit_write_failures_total"
---

<objective>
Expose the audit trail: RBAC-gated query API with date/actor/action/resource filters, streaming CSV export, chain-verification endpoint, OTel metrics, a Grafana "Audit Trail" row, and the docker-compose env vars that make orchestrator and dashboard share one audit DB (REQ-012, closes D-02's "verification exposed as a check").
</objective>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-audit-trail/07-CONTEXT.md

<interfaces>
<!-- Verified against the codebase. -->

shared/auth/rbac.py — Permission enum :14-20; ROLE_PERMISSIONS :23-33; ADMIN lacks MANAGE_CREDENTIALS by design;
OWNER = set(Permission) auto-includes new members. Add READ_AUDIT = "read_audit" and grant to ADMIN only.

orchestrator/admin_api.py — append the three GET endpoints at END of file (minimizes conflict surface).
Handlers: user = Depends(require_permission(Permission.READ_AUDIT)); org scoping: forced org_id=user.org_id unless
user.role == Role.OWNER (Role imported from shared.auth). Query params: start, end (ISO-8601), actor_id, action,
resource_type, resource_id, limit (default 100, cap 1000), offset. Response {"events", "count", "total", "limit",
"offset"}. Export: fastapi.responses.StreamingResponse, media_type="text/csv", Content-Disposition attachment
filename audit_events_<UTC yyyy-mm-dd>.csv; iterate store.iter_events(batch_size=1000); csv module writes to an
io.StringIO flushed per batch; injection guard: any cell whose str starts with = + - @ gets a leading '.
Columns: id,timestamp,org_id,actor_id,action,resource_type,resource_id,ip_address,before_state,after_state,details,prev_hash,row_hash.
Verify endpoint returns dataclasses.asdict(ChainVerificationResult).

shared/observability/metrics.py — OTel (NOT prometheus_client): meter = get_meter("grpc_llm") :21; follow the
ModuleMetrics dataclass + create_module_metrics() factory (:467/:496). Add AuditMetrics(audit_events_total,
audit_write_failures_total) + create_audit_metrics(meter=None). AuditStore integrates via a lazy module-level
holder guarded so environments without OTel wiring still work (mirror how other stores degrade); increment
events_total (attrs action, resource_type, channel from actor ctx or "system") on success, write_failures_total
before re-raising AuditWriteError.

Grafana — config/grafana/provisioning/dashboards/json/nexus-modules.json: 16 panels, next free id 17, last row
ends y=32 h=8 → new row at y=40. Datasource {type: "prometheus", uid: "prometheus"} as in existing panels.
Panels: id 17 row "Audit Trail" {h:1,w:24,x:0,y:40}; id 18 timeseries "Audit Events by Action"
sum by (action) (rate(nexus_audit_events_total[5m])) {h:8,w:12,x:0,y:41}; id 19 stat "Audit Events (24h)"
sum(increase(nexus_audit_events_total[24h])) {h:8,w:6,x:12,y:41}; id 20 stat "Audit Write Failures (24h)"
sum(increase(nexus_audit_write_failures_total[24h])) thresholds red>0 {h:8,w:6,x:18,y:41}. Metric names may
surface with the grpc_llm_ prefix and _total suffix via the OTel exporter — match the naming convention used by
existing nexus_module_* panels in this file.

docker-compose.yaml — orchestrator env block ~:128: add AUDIT_DB_PATH=/app/data/audit_events.db; dashboard env
block ~:167: add AUDIT_DB_PATH=/app/shared_data/audit_events.db (dashboard's /app/data is ./dashboard_service/data —
the env var is what makes both hit host ./data/audit_events.db; same trick as AUTH_DB_PATH).

Makefile — finalize audit-test to run all four audit test files (unit/test_audit_store.py, unit/test_audit_decorator.py,
integration/admin/test_audit_capture.py, integration/admin/test_audit_query_api.py). @printf, not @echo.

tests/integration/admin/conftest.py — extend create_test_admin_app with the three real audit endpoints (wire against
app.state / the audit_store fixture, mirroring how other endpoints are replicated there).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: READ_AUDIT permission + query/export/verify endpoints</name>
  <files>shared/auth/rbac.py, orchestrator/admin_api.py, tests/integration/admin/test_audit_query_api.py, tests/integration/admin/conftest.py</files>
  <read_first>
    - shared/auth/rbac.py (57 lines, full)
    - orchestrator/admin_api.py (tail of file + the _audit_store global from 07-02)
    - tests/integration/admin/conftest.py (audit fixtures from 07-02)
  </read_first>
  <behavior>
    - READ_AUDIT in Permission; ADMIN granted; VIEWER/OPERATOR excluded; OWNER automatic
    - Query endpoint: filter combinations, pagination totals, ORDER BY id DESC
    - Org scoping: admin of org A cannot see org B events; owner may pass ?org_id=
    - RBAC: viewer/operator → 403; admin/owner → 200 (all three endpoints)
    - CSV export: header row, correct columns, injection-guarded cells, Content-Disposition
    - Verify endpoint: valid=True on intact chain; tampered scratch DB → valid=False with first_invalid_id
  </behavior>
  <action>
    Add the permission, the three endpoints per the interface notes, mirror them into the test app factory, and write test_audit_query_api.py covering every behavior with the per-role key fixtures.
  </action>
  <verify>
    <automated>cd tests && python -m pytest integration/admin/test_audit_query_api.py -x -q && python -m pytest unit/test_auth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - RBAC matrix asserted for all three endpoints
    - CSV test asserts a cell beginning with "=" is exported with a leading apostrophe
    - Tamper test asserts first_invalid_id in the verify response
  </acceptance_criteria>
  <done>Audit read surface complete and RBAC-gated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Metrics, Grafana row, compose env, Makefile finalization</name>
  <files>shared/observability/metrics.py, shared/audit/store.py, config/grafana/provisioning/dashboards/json/nexus-modules.json, docker-compose.yaml, Makefile</files>
  <read_first>
    - shared/observability/metrics.py (:460-560 — ModuleMetrics pattern)
    - config/grafana/provisioning/dashboards/json/nexus-modules.json (panel/datasource/naming conventions — check how nexus_module_* metrics are referenced)
    - docker-compose.yaml (:120-190 — both env blocks)
  </read_first>
  <behavior>
    - AuditMetrics + create_audit_metrics follow the existing factory pattern; AuditStore increments lazily and degrades without OTel
    - Grafana JSON stays valid (json.load), panel ids unique, row renders per the interface spec
    - Both services get AUDIT_DB_PATH pointing at the same host file
    - make audit-test runs all four audit test files
  </behavior>
  <action>
    Implement per the interface notes. After editing the Grafana JSON, validate with python -c "import json; json.load(open('config/grafana/provisioning/dashboards/json/nexus-modules.json'))" and assert unique panel ids.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('config/grafana/provisioning/dashboards/json/nexus-modules.json')); ids=[p['id'] for p in d['panels']]; assert len(ids)==len(set(ids)), 'duplicate panel ids'" && python -c "import yaml; yaml.safe_load(open('docker-compose.yaml'))" && make audit-test</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "AUDIT_DB_PATH" docker-compose.yaml shows both services with the correct container paths
    - grep -c "audit" Makefile shows the finalized target; make audit-test green
    - Metrics unit-verifiable: recording an event increments the counter when a meter is available (in-memory reader or guarded no-op assertion)
  </acceptance_criteria>
  <done>Observability + deployment wiring complete; full audit suite green.</done>
</task>

</tasks>

<verification>
- `make audit-test` green (all four files); `cd tests && python -m pytest integration/admin/ -q` green
- Manual E2E (documented in SUMMARY, executed if services are running): docker compose up → mutate a module via Admin API and via chat → GET /admin/audit-logs shows both with correct actor/channel → /verify valid → CSV downloads redacted rows → Grafana Audit Trail row renders
</verification>

<success_criteria>
- REQ-012 delivered: filtered query, CSV export, chain verification, Grafana panel; audit reads RBAC-gated via READ_AUDIT; one shared audit DB across services
</success_criteria>

<output>
Create `.planning/phases/07-audit-trail/07-03-SUMMARY.md` when done.
</output>
