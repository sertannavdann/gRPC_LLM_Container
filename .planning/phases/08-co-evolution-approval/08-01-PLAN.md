---
phase: 08-co-evolution-approval
plan: 01
type: execute
wave: 2
depends_on: ["08-02", "08-04"]
files_modified:
  - shared/modules/policy.py
  - shared/modules/approval.py
  - tools/builtin/module_installer.py
  - orchestrator/admin_api.py
  - tests/unit/modules/test_installer_approval_guard.py
  - tests/integration/admin/test_approval_gate.py
autonomous: true
requirements: ["REQ-014"]
must_haves:
  truths:
    - "install_module() refuses to install any module whose status is not APPROVED (D-16 backend enforcement)"
    - "An admin (WRITE_CONFIG) can approve a VALIDATED module, moving it to APPROVED (D-16, D-17)"
    - "Approve/reject below admin+ role returns 403 (D-17)"
    - "Reject-with-feedback sends the module back through one bounded repair cycle and returns it to pending approval (D-09)"
    - "Reject-without-feedback is terminal: status becomes FAILED and the module is queued for artifact GC (D-09, D-10)"
    - "Every approve and reject decision is recorded with actor, timestamp, and bundle hash (D-19)"
    - "An ApprovalPolicy config object exists with auto_approve defaulting to False as a hard default (D-18)"
  artifacts:
    - path: "shared/modules/policy.py"
      provides: "ApprovalPolicy Pydantic model, auto_approve=False hard default"
      contains: "class ApprovalPolicy"
    - path: "shared/modules/approval.py"
      provides: "approve_module / reject_module core functions reused by HTTP + chat"
      exports: ["approve_module", "reject_module"]
    - path: "orchestrator/admin_api.py"
      provides: "POST approve/reject endpoints + GET review/audit read endpoints"
    - path: "tools/builtin/module_installer.py"
      provides: "install attestation guard raised from VALIDATED to APPROVED"
      contains: "ModuleStatus.APPROVED"
  key_links:
    - from: "orchestrator/admin_api.py approve/reject endpoints"
      to: "shared/modules/approval.py"
      via: "direct function call after RBAC dependency resolves"
      pattern: "approve_module|reject_module"
    - from: "shared/modules/approval.py reject_module (no feedback)"
      to: "shared/modules/gc.py queue_for_gc"
      via: "import + call in terminal-reject branch"
      pattern: "queue_for_gc"
    - from: "tools/builtin/module_installer.py install guard"
      to: "shared.modules.manifest.ModuleStatus.APPROVED"
      via: "status comparison before install"
      pattern: "status != ModuleStatus.APPROVED"
---

<objective>
Deliver the backend enforcement core of the module approval gate: the install-time attestation guard (VALIDATED → APPROVED), the reusable approve/reject core functions, the HTTP endpoints (approve, reject, review-read, audit-read), and the `auto_approve: false` policy scaffold. This is the mechanism that makes the phase done-criterion — "no module installed without explicit user approval" — true at the enforcement layer, not just in the UI.

Purpose: D-16 requires the approval gate be enforced in `install_module()`, so no chat agent, tool, or endpoint can install an unapproved module. D-09/D-10/D-19 govern the reject semantics and audit trail. D-18 scaffolds the policy object for future auto-approve relaxation.
Output: `shared/modules/policy.py`, `shared/modules/approval.py`, install-guard change, four admin endpoints, two test files.
</objective>

<source_audit>
## Multi-Source Coverage Audit (phase-wide, maintained in the lead plan)

| Source | Item | Covered By | Status |
|--------|------|-----------|--------|
| GOAL | "No module installed without explicit user approval" | 08-01 (install guard + approve/reject) | COVERED |
| GOAL | "Traces auto-purge per retention policy" | 08-02 (retention worker) | COVERED |
| REQ | REQ-014 Module approval gates (UI review + approve/reject feedback loop) | 08-01, 08-04, 08-05, 08-06, 08-07, 08-08 | COVERED |
| REQ | REQ-017 Tiered trace retention + daily cleanup worker | 08-02 | COVERED |
| REQ | REQ-018 Pipeline SSE E2E tests (connect, reconnect, error) | 08-09 | COVERED |
| REQ | REQ-020 Rate limiting (token bucket, 429 + Retry-After, all HTTP) | 08-03 | COVERED |
| RESEARCH | Status-guard change VALIDATED→APPROVED (Pattern 1) | 08-01 | COVERED |
| RESEARCH | SSE payload scans manifests, not just loaded modules (Pattern 3) | 08-05 | COVERED |
| RESEARCH | pipelinePageMachine wraps connectPipelineSSE via fromCallback (Pattern 4) | 08-06 | COVERED |
| RESEARCH | Rate-limit middleware keys on X-API-Key header (Pattern 5, Pitfall 3) | 08-03 | COVERED |
| RESEARCH | App-level SQLite retention (A1), TIER_RETENTION_DAYS convention | 08-02 | COVERED |
| RESEARCH | Reference-safety before delete (Pitfall 5, D-12) | 08-02 | COVERED |
| RESEARCH | Purpose.CRITIC lane for walkthrough (A3) | 08-04 | COVERED |
| RESEARCH | Playwright against real services, not mocked EventSource (Pitfall 4) | 08-09 | COVERED |
| CONTEXT | D-01 review on pipeline page / VALIDATED → pending node | 08-05, 08-06, 08-07 | COVERED |
| CONTEXT | D-02 chat approval + blueprint | 08-08 | COVERED |
| CONTEXT | D-03 SSE 2s transport | 08-05, 08-06 | COVERED |
| CONTEXT | D-04 graph is the notification (pending badge) | 08-06 | COVERED |
| CONTEXT | D-05 progressive blueprint (card → mini-graph) | 08-07 | COVERED |
| CONTEXT | D-06 five review layers (collapsible) | 08-07 | COVERED |
| CONTEXT | D-07 generic auto-generated module UI | 08-08 | COVERED |
| CONTEXT | D-08 LLM walkthrough gen + display | 08-04 (gen), 08-07 (display) | COVERED |
| CONTEXT | D-09 reject-with-feedback repair / reject-without terminal | 08-01 | COVERED |
| CONTEXT | D-10 terminal rejection purges heavyweight artifacts | 08-02 (purge), 08-01 (queue) | COVERED |
| CONTEXT | D-11 one shared background GC/retention worker | 08-02 | COVERED |
| CONTEXT | D-12 GC reference-safety | 08-02 | COVERED |
| CONTEXT | D-13 live build-stage StageNode progression | 08-06 | COVERED |
| CONTEXT | D-14 pipelinePageMachine (XState v5) | 08-06 | COVERED |
| CONTEXT | D-15 per-attempt timeline from BuildAuditLog | 08-07 | COVERED |
| CONTEXT | D-16 always-manual, backend-enforced | 08-01 | COVERED |
| CONTEXT | D-17 approve/reject admin+ RBAC (HTTP + chat) | 08-01 (HTTP), 08-08 (chat) | COVERED |
| CONTEXT | D-18 policy scaffold auto_approve=false | 08-01 | COVERED |
| CONTEXT | D-19 record every decision (actor/time/hash) | 08-01 | COVERED |

No unplanned items. Deferred Ideas (module versioning UI, marketplace, auto-approve activation, multi-approver) are out of scope per CONTEXT.md.
</source_audit>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-co-evolution-approval/08-CONTEXT.md
@.planning/phases/08-co-evolution-approval/08-RESEARCH.md
@.planning/phases/08-co-evolution-approval/08-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; do not re-explore. -->

shared/modules/manifest.py — ModuleStatus(str, Enum): DRAFT, VALIDATING="validating", VALIDATED="validated",
APPROVED="approved", INSTALLED?, FAILED="failed". ModuleManifest.load(manifest_file)/save(modules_dir).

tools/builtin/module_installer.py (lines 96-106) — current guard:
`if manifest.status != ModuleStatus.VALIDATED and manifest.status != ModuleStatus.VALIDATED.value:` then
`_log_install_rejection(module_id, "not_validated", ...)` and returns {"status":"error","error":...}.
`_log_install_rejection(module_id, reason, details)` helper defined at line 264.

orchestrator/admin_api.py — `_app = FastAPI(...)`; `_draft_manager`/`_version_manager` module globals;
active draft endpoints at lines 636-700 use `Depends(require_permission(Permission.WRITE_CONFIG))` (this is the
"admin+" permission — OPERATOR lacks WRITE_CONFIG, ADMIN has it). Error pattern:
`if result.get("status") != "success": raise HTTPException(400, result.get("error","..."))`.
NOTE: admin_api.py contains a SECOND duplicate validate_draft/promote_draft block (lines ~1458-1502) using
ADMIN_ALL — do NOT add a third duplicate; add new endpoints adjacent to the ACTIVE block near line 700 and use
WRITE_CONFIG. DevModeAuditLog instance is reachable as `_draft_manager.audit_log` (same instance shared with
VersionManager); it exposes `.log_action(action, actor, module_id, draft_id=None, details={...})`.

shared/auth/rbac.py — Permission enum: READ_CONFIG, WRITE_CONFIG, MANAGE_MODULES, MANAGE_CREDENTIALS,
MANAGE_KEYS, ADMIN_ALL. Role matrix: VIEWER={READ_CONFIG}, OPERATOR adds MANAGE_MODULES,
ADMIN adds WRITE_CONFIG. So WRITE_CONFIG == admin+, MANAGE_MODULES == operator+.

shared/modules/gc.py (created by plan 08-02, dependency) — exports `queue_for_gc(module_id, modules_dir)`
which writes a `.gc_pending` marker JSON `{module_id, rejected_at, actor}` inside the module dir.

tools/builtin/module_builder.py — `repair_module(...)` exists (bounded MAX_REPAIR_ATTEMPTS=10) and is the
D-09 repair entry point; it reads the module's BuildAuditLog and calls gateway.generate(purpose=REPAIR).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raise install guard to APPROVED + scaffold ApprovalPolicy</name>
  <files>tools/builtin/module_installer.py, shared/modules/policy.py, tests/unit/modules/test_installer_approval_guard.py</files>
  <read_first>
    - tools/builtin/module_installer.py (lines 47-170 — install_module body, current VALIDATED guard at 96-106, _log_install_rejection at 264)
    - shared/modules/manifest.py (ModuleStatus enum lines 16-24 — confirm APPROVED="approved" exists)
    - orchestrator/routing_config.py (Pydantic config-model style to mirror for policy.py)
    - tests/unit/modules/test_rollback_pointer.py (tmp_path + manifest fixture style to mirror for the new test)
  </read_first>
  <behavior>
    - install_module() on a module whose manifest.status == VALIDATED returns {"status":"error"} with error text containing "not been approved" and logs rejection reason "not_approved"
    - install_module() proceeds past the status guard only when manifest.status == APPROVED (attestation/hash checks downstream unchanged)
    - ApprovalPolicy().auto_approve is False by default; ApprovalPolicy(auto_approve=True).auto_approve is True (field exists, default is the hard False)
  </behavior>
  <action>
    In tools/builtin/module_installer.py change the pre-install guard (currently lines 96-106) so the compared status is `ModuleStatus.APPROVED` instead of `ModuleStatus.VALIDATED` (compare against both the enum and its `.value` as the existing line does), per D-16. Change the `_log_install_rejection` reason string from "not_validated" to "not_approved" and the error message to state the module has not been approved for install and that an admin must approve it first. Do not alter the downstream bundle_sha256 attestation/hash-match logic — only the status the guard requires changes.

    Create shared/modules/policy.py with a Pydantic `ApprovalPolicy(BaseModel)` following the `orchestrator/routing_config.py` model style (NOT a dataclass, and do NOT add `from __future__ import annotations` — that breaks Pydantic in this repo per project memory). Fields per D-18: `auto_approve: bool = False` (hard default), plus forward-compatible scaffold fields `trusted_categories: list[str] = []` and `require_all_tests_pass: bool = True`. Add a module-level default `DEFAULT_APPROVAL_POLICY = ApprovalPolicy()`. This is a scaffold only — no auto-approve code path is wired in this phase.

    Create tests/unit/modules/test_installer_approval_guard.py mirroring the tmp_path/manifest fixture approach in test_rollback_pointer.py: build a module dir with a manifest at status VALIDATED, assert install_module rejects with reason "not_approved"; set status APPROVED and assert the guard is passed (installer proceeds past the status check). Add an ApprovalPolicy assertion for the auto_approve=False default.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/modules/test_installer_approval_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ModuleStatus.APPROVED" tools/builtin/module_installer.py` shows the guard comparison uses APPROVED
    - `grep -n "not_approved" tools/builtin/module_installer.py` returns the rejection reason
    - `python -c "from shared.modules.policy import ApprovalPolicy; assert ApprovalPolicy().auto_approve is False"` exits 0
    - `grep -v '^#' shared/modules/policy.py | grep -c "from __future__ import annotations"` returns 0
    - test file passes; a VALIDATED-status module is rejected and an APPROVED-status module clears the guard
  </acceptance_criteria>
  <done>install_module refuses non-APPROVED modules; ApprovalPolicy scaffold exists with auto_approve=False default; unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Approve/reject core functions + admin endpoints + read endpoints</name>
  <files>shared/modules/approval.py, orchestrator/admin_api.py, tests/integration/admin/test_approval_gate.py</files>
  <read_first>
    - orchestrator/admin_api.py (lines 636-720 — active draft endpoint block, RBAC + error patterns; and lines ~1458-1502 to AVOID the duplicate block)
    - shared/modules/approval.py (does not exist yet — you are creating it)
    - shared/modules/gc.py (created by 08-02 — confirm `queue_for_gc(module_id, modules_dir)` signature)
    - tools/builtin/module_builder.py (repair_module signature + how it loads BuildAuditLog for a module — D-09 repair path)
    - shared/modules/manifest.py (ModuleManifest.load/save, ValidationResults shape for the review endpoint payload)
    - tests/integration/admin/conftest.py (admin_headers/operator_headers fixtures + create_test_admin_app factory)
    - tests/integration/admin/test_module_crud.py (RBAC test + endpoint-call pattern to mirror)
  </read_first>
  <behavior>
    - approve_module(module_id, actor, audit_log, modules_dir): only a VALIDATED module transitions to APPROVED; a non-VALIDATED module returns {"status":"error"}; on success logs action "module_approved" with actor, timestamp, and bundle_sha256
    - reject_module with non-empty feedback: sets status to VALIDATING, invokes one bounded repair cycle (repair_module), logs "module_rejected" with terminal=false; returns success
    - reject_module with empty/None feedback: sets status FAILED, calls queue_for_gc(module_id, modules_dir), logs "module_rejected" with terminal=true; returns success
    - POST /admin/modules/{module_id}/approve and /reject require WRITE_CONFIG: viewer/operator key → 403, admin key → 200
    - GET /admin/modules/{module_id}/review returns status, validation_results, walkthrough, requested credentials, and a blueprint structure summary; GET .../audit returns BuildAuditLog attempt records — both readable by MANAGE_MODULES (operator+)
  </behavior>
  <action>
    Create shared/modules/approval.py with two pure functions callable from BOTH the HTTP endpoints and the chat strategy (08-08), so approval logic is defined once: `approve_module(module_id, actor, audit_log, modules_dir)` and `reject_module(module_id, feedback, actor, audit_log, modules_dir)`. approve_module loads the manifest, guards `manifest.status == ModuleStatus.VALIDATED` (else return error dict), sets APPROVED, saves, and calls `audit_log.log_action(action="module_approved", actor=actor, module_id=module_id, details={"bundle_sha256": <current bundle hash>, "timestamp": <iso8601 utc>})` per D-19. reject_module: if feedback is non-empty, set status VALIDATING, call `repair_module` for the module id (feed the feedback text as repair hints into the existing repair path per D-09), log "module_rejected" with terminal=false; if feedback is empty/None, set status FAILED, import and call `queue_for_gc(module_id, modules_dir)` from shared.modules.gc (D-10), log "module_rejected" with terminal=true. Both return `{"status":"success", "module_id":..., "new_status":...}`. Resolve the bundle hash via the existing artifact bundling surface used by install_module (shared/modules/artifacts.py); if no bundle exists yet, record None.

    In orchestrator/admin_api.py, adjacent to the ACTIVE draft endpoint block (near line 700 — do NOT touch or duplicate the second block at ~1458), add: a `RejectModuleRequest(BaseModel)` with `feedback: Optional[str] = None`; `POST /admin/modules/{module_id}/approve` and `POST /admin/modules/{module_id}/reject` both gated by `Depends(require_permission(Permission.WRITE_CONFIG))` (admin+, matching the active validate_draft/promote_draft, per D-17), each calling the approval.py functions with `actor=user.org_id` and `audit_log=_draft_manager.audit_log`, and applying the file's standard `if result.get("status") == "error": raise HTTPException(400, ...)` pattern. Add read endpoints `GET /admin/modules/{module_id}/review` and `GET /admin/modules/{module_id}/audit` gated by `Depends(require_permission(Permission.MANAGE_MODULES))` (operator+): review returns a JSON dict with keys `status`, `validation_results`, `walkthrough` (read via getattr(manifest,"walkthrough","") — the field is added by 08-04), `credentials` (manifest credential requirements), and `blueprint` (a structure summary: adapter name, schema field count, output types, credential names for the D-05 mini-graph); audit returns the module's BuildAuditLog attempt records as a list. Import `from shared.modules.manifest import ModuleManifest, ModuleStatus` and `from shared.modules.approval import approve_module, reject_module` at the top.

    Create tests/integration/admin/test_approval_gate.py using the conftest fixtures: extend create_test_admin_app to expose the new endpoints; assert operator_headers and viewer_headers get 403 on approve/reject, admin_headers gets 200; assert approving a VALIDATED module yields new_status "approved"; assert reject-without-feedback yields new_status "failed" and reject-with-feedback yields "validating"; assert the audit log records a "module_approved" entry with actor + bundle_sha256 keys (D-19).
  </action>
  <verify>
    <automated>python -m pytest tests/integration/admin/test_approval_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def approve_module\|def reject_module" shared/modules/approval.py` shows both functions
    - `grep -n "queue_for_gc" shared/modules/approval.py` confirms terminal-reject imports the GC queue (D-10)
    - `grep -n "require_permission(Permission.WRITE_CONFIG)" orchestrator/admin_api.py | wc -l` increases by the two new mutation endpoints
    - approve/reject return 403 for operator+viewer keys and 200 for admin key in the test
    - test asserts audit entry "module_approved" carries actor and bundle_sha256 (D-19)
    - `grep -c "def promote_draft" orchestrator/admin_api.py` is unchanged (no new duplicate added)
  </acceptance_criteria>
  <done>Approve/reject core + endpoints enforce D-16/D-17/D-09/D-19; review/audit reads serve the UI; integration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat agent / HTTP client → admin API | untrusted actor requests a state-changing approval or an install |
| approved manifest on disk → install_module | attestation gate; a tampered/unapproved manifest must not install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | install_module status guard | mitigate | Guard requires ModuleStatus.APPROVED (Task 1); no code path installs a non-APPROVED module (D-16) |
| T-08-02 | Elevation of Privilege | approve/reject endpoints | mitigate | `require_permission(Permission.WRITE_CONFIG)` (admin+) on both mutations; test asserts 403 for operator/viewer (D-17) |
| T-08-03 | Tampering | artifact between validate and install | mitigate | Existing bundle_sha256 attestation in install_module is unchanged; approval only gates it one status later (guard now APPROVED) |
| T-08-04 | Repudiation | approval decisions | mitigate | Every approve/reject logged via DevModeAuditLog with actor + timestamp + bundle_sha256 (D-19, append-only JSONL) |
| T-08-05 | Information Disclosure | review/audit read endpoints | accept | Review payload exposes generated code metadata to operator+ only; no raw credentials returned (only credential names/requirements), consistent with existing admin read scope |
</threat_model>

<verification>
- `python -m pytest tests/unit/modules/test_installer_approval_guard.py tests/integration/admin/test_approval_gate.py -x -q` green
- Manual grep gate: `grep -v '^#' tools/builtin/module_installer.py | grep -c "ModuleStatus.VALIDATED"` for the install guard region shows the guard no longer requires VALIDATED
</verification>

<success_criteria>
- No module installs unless APPROVED (D-16); approve/reject are admin+ only (D-17)
- Reject-with-feedback triggers one repair cycle; reject-without-feedback is terminal + GC-queued (D-09/D-10)
- Every decision audited with actor/timestamp/hash (D-19); ApprovalPolicy scaffold present (D-18)
</success_criteria>

<output>
Create `.planning/phases/08-co-evolution-approval/08-01-SUMMARY.md` when done.
</output>
