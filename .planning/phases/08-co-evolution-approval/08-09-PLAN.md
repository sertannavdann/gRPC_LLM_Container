---
phase: 08-co-evolution-approval
plan: 09
type: execute
wave: 4
depends_on: ["08-08", "08-06"]
files_modified:
  - ui_service/package.json
  - ui_service/src/components/pipeline/BlueprintCard.tsx
  - ui_service/src/components/pipeline/NodeDetailPanel.tsx
  - ui_service/src/app/pipeline/page.tsx
autonomous: false
requirements: ["REQ-014"]
user_setup: []

must_haves:
  truths:
    - "Selecting a pending-approval module shows all five review layers as collapsible sections: blueprint, code diff, sandbox validation report, requested credentials, repair history (D-06)"
    - "The blueprint renders as a technical-drawing summary card that expands into an interactive React Flow mini-graph of adapter → schema → outputs → credentials (D-05)"
    - "The LLM walkthrough is displayed above the sections as a distinct AI-generated block (D-08 display half)"
    - "The repair-history section shows one row per BuildAuditLog attempt with attempt number, stage, status and failure fingerprint (D-15)"
    - "An admin can approve, or reject with optional feedback, from the panel footer; terminal rejection requires a second explicit confirmation click"
    - "A section whose data fails to load renders an explicit error affordance instead of silently showing nothing"
  artifacts:
    - path: "ui_service/src/components/pipeline/BlueprintCard.tsx"
      provides: "collapsed technical-drawing summary + expandable read-only React Flow mini-graph"
      contains: "BlueprintCard"
    - path: "ui_service/src/components/pipeline/NodeDetailPanel.tsx"
      provides: "five accordion review sections + walkthrough block + approve/reject footer"
      contains: "Accordion"
  key_links:
    - from: "ui_service/src/components/pipeline/NodeDetailPanel.tsx"
      to: "pipelinePageMachine reviewPanel region"
      via: "review/audit context props + APPROVE/REJECT events"
      pattern: "APPROVE|REJECT"
    - from: "ui_service/src/app/pipeline/page.tsx"
      to: "NodeDetailPanel review props"
      via: "state.context.review / state.context.audit passed down"
      pattern: "state.context.review"
---

<objective>
Build the review surface itself (REQ-014's "UI review of generated code + required credentials + sandbox test results"): the five collapsible layers of D-06, the progressive blueprint of D-05, the walkthrough display of D-08, the per-attempt timeline of D-15, and the approve/reject footer.

Purpose: this is the half of REQ-014 that verification marked entirely unbuilt — the backend approve/reject mechanics exist and work, but no human-facing review of code, credentials, or sandbox results has ever existed.
Output: `BlueprintCard.tsx`, an extended `NodeDetailPanel.tsx`, and the page wiring that feeds them.
</objective>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-co-evolution-approval/08-UI-SPEC.md
@.planning/phases/08-co-evolution-approval/08-PATTERNS.md
@.planning/phases/08-co-evolution-approval/08-CONTEXT.md

<interfaces>
<!-- Contracts extracted from the codebase. Do not re-explore. -->

ui_service/src/components/pipeline/NodeDetailPanel.tsx (313 lines) —
props `{ node: SelectedNode | null; onClose: () => void; testRunning: boolean; testResult: TestRunResult | null;
onRunTests?: (category, platform) => void }`; `typeConfig` map keyed on node type (stage/tool/adapter/service —
`module` is NOT present and must be added); `StatusDot` helper; the shell is
`<AnimatePresence>{node && (<><motion.div key="backdrop" .../><motion.div key="panel" initial={{x:'100%'}}
animate={{x:0}} exit={{x:'100%'}} transition={{type:'spring',damping:25,stiffness:300}}
className="fixed right-0 top-0 bottom-0 w-96 bg-zinc-900 border-l border-zinc-700 z-50 flex flex-col shadow-2xl">`
with a header row and then `<div className="flex-1 overflow-auto p-4 space-y-4">` (line ~139) — the content area
new sections go INSIDE. Section headers use
`<h3 className="text-xs font-medium text-zinc-400 uppercase tracking-wide mb-2">`. Test output uses
`<pre className="text-[11px] text-zinc-400 bg-zinc-950 rounded p-2 overflow-auto max-h-60 whitespace-pre-wrap font-mono">`.
Pass/fail rows use `CheckCircle2` (green-400) / `XCircle` (red-400). Auth rows use `Lock`/`Unlock`.

ui_service/src/lib/adminClient.ts (plan 08-07) — `ModuleReview { module_id, status, validation_results,
walkthrough, credentials: { requires_api_key, auth_type, api_key_instructions }, blueprint: ModuleBlueprint }`,
`ModuleBlueprint { adapter_name, schema_field_count, output_types, credential_names }`,
`ModuleAuditAttempt` (typed from `shared/modules/audit.py::AttemptRecord`: attempt_number, bundle_sha256,
stage, status, failure_fingerprint, failure_type, logs, metadata + timestamp), `ModuleAuditLog`.

ui_service/src/machines/pipelinePage.ts (plan 08-07) — context `{ review, audit, actionError, selectedModuleId, ... }`;
events `APPROVE`, `REJECT {feedback?}`, `RETRY`, `CLOSE_PANEL`; reviewPanel region states
`closed | loading | open | approving | rejecting | error`.

ui_service/src/components/ui/error-states.tsx — `EmptyState` (lines 84-107, `Inbox`-style icon + centered
title/description) and `DegradedBanner` (amber icon + message + optional Retry) to REUSE VERBATIM for the
empty and per-section error states per UI-SPEC's Copywriting Contract.

ui_service/src/components/monitoring/PipelineStageFlow.tsx — `STAGE_COLORS` (scaffold #3b82f6, implement
#8b5cf6, test #f59e0b, repair #ef4444) for the repair-history stage labels; read-only React Flow config
(`nodesDraggable={false}`, `elementsSelectable={false}`) to mirror in the blueprint mini-graph.

ui_service/src/components/pipeline/StageNode.tsx — the dashed technical-drawing aesthetic
(`rounded-xl border-2 border-dashed px-5 py-3 min-w-[150px]`) that UI-SPEC §3 mandates the BlueprintCard reuse.

Dependencies already installed: `@xyflow/react` 12.10.0, `framer-motion` 11.18.2, `lucide-react` 0.462.0,
`react-markdown` 9.0.1, `remark-gfm` 4.0.0, `@radix-ui/react-avatar|scroll-area|slot`.
`ui_service/tailwind.config.ts` ALREADY provisions `accordion-down`/`accordion-up` keyframes.
NEW dependency required by UI-SPEC §2/D-06: `@radix-ui/react-accordion`.

Code diff data source (accordion section 2): the diff endpoint belongs to the DRAFT lifecycle
(`DraftManager.get_diff` via the admin draft endpoints), and a freshly built module (Path A) has NO draft.
Read `orchestrator/admin_api.py`'s draft-diff endpoint and `shared/modules/drafts.py` before wiring: if no
draft exists for the module, section 2 must render the adapter source itself (or an explicit
"No draft diff — this module was built fresh" affordance), NOT an empty box.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this gate precedes a package-manager install.</what-built>
  <how-to-verify>
    `08-RESEARCH.md`'s Package Legitimacy Audit table covers only `@playwright/test`. `@radix-ui/react-accordion`
    (required by UI-SPEC §2 for D-06's five collapsible sections) is NOT in that table, so under the fallback
    policy it is treated as [ASSUMED] and must be human-verified before install.

    1. Open https://www.npmjs.com/package/@radix-ui/react-accordion
    2. Confirm: publisher scope is `@radix-ui` (the same first-party scope as the already-installed
       `@radix-ui/react-avatar`, `@radix-ui/react-scroll-area`, `@radix-ui/react-slot`), repository is
       github.com/radix-ui/primitives, weekly downloads are in the millions, and the package has years of
       release history.
    3. Confirm the version resolved by `npm view @radix-ui/react-accordion version` is a current release.
    4. If slopcheck is available, `slopcheck install @radix-ui/react-accordion --ecosystem npm` and confirm `[OK]`.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install, or "rejected" to have the executor implement the five collapsible sections with a local state-driven disclosure component using the accordion keyframes already present in tailwind.config.ts instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install accordion primitive + BlueprintCard (D-05)</name>
  <files>ui_service/package.json, ui_service/src/components/pipeline/BlueprintCard.tsx</files>
  <read_first>
    - ui_service/package.json (dependency list and the existing @radix-ui pins)
    - ui_service/src/components/pipeline/StageNode.tsx (full — the dashed technical-drawing shell to reuse)
    - ui_service/src/components/monitoring/PipelineStageFlow.tsx (lines 100-170 — read-only ReactFlow config and static node positioning)
    - ui_service/src/app/pipeline/page.tsx (lines 40-56 — `STAGE_NODES` static x-offset positioning style to mirror)
    - .planning/phases/08-co-evolution-approval/08-UI-SPEC.md (Component Contract §3 — exact collapsed/expanded specification)
  </read_first>
  <action>
    Only after the gate above resolves "approved": run `npm install @radix-ui/react-accordion` inside `ui_service/`
    and commit the resulting `package.json` (and lockfile if one is tracked). If the gate resolved "rejected",
    skip the install and note the substitution in the SUMMARY.

    Create `ui_service/src/components/pipeline/BlueprintCard.tsx` exporting `BlueprintCard` with props
    `{ blueprint: ModuleBlueprint; moduleName: string; expandable?: boolean }` (default `expandable = true`).

    Collapsed state (also the ONLY state used by the chat card in plan 08-11, hence the `expandable` prop):
    a card with `border border-dashed border-zinc-600/50 bg-zinc-950/40 rounded-lg p-3`, a monospace module-name
    header, and a 4-row summary list — Adapter (`blueprint.adapter_name`), Schema (`schema_field_count` fields),
    Outputs (`output_types.length`, with the distinct type names shown), Credentials (`credential_names.length`) —
    each row a small dot + label + count using the panel's existing `flex items-center justify-between text-sm`
    row rhythm. When `expandable`, add an "Expand blueprint" text button (`text-orange-400 text-xs` + `ChevronDown`).

    Expanded state: `AnimatePresence` height transition into a fixed `h-64` embedded `<ReactFlow>` with
    `nodesDraggable={false} elementsSelectable={false} proOptions={{ hideAttribution: true }} fitView`, rendering a
    static horizontal 4-node chain Adapter → Schema → Outputs → Credentials at fixed x offsets (no layout
    algorithm — UI-SPEC Flag 2), each node reusing the dashed stage aesthetic at reduced scale
    (`min-w-[100px]`, `text-[11px]`), connected by non-animated `zinc-600` edges. Define the mini node type
    locally in this file; do not register it in the page's global `nodeTypes`.

    Handle the empty case explicitly: when every blueprint count is zero, render the summary rows with `0` plus a
    muted note that the adapter could not be statically summarized — never render an empty card (Phase 6 no-silent-fallback).
  </action>
  <verify>
    <automated>cd ui_service && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@radix-ui/react-accordion" ui_service/package.json` returns a match (or the SUMMARY documents the rejected-gate substitution)
    - `grep -n "border-dashed" ui_service/src/components/pipeline/BlueprintCard.tsx` returns a match (StageNode aesthetic reused)
    - `grep -n "nodesDraggable={false}" ui_service/src/components/pipeline/BlueprintCard.tsx` returns a match
    - `grep -n "expandable" ui_service/src/components/pipeline/BlueprintCard.tsx` shows the prop guarding the expand affordance
    - `cd ui_service && npm run build` succeeds
  </acceptance_criteria>
  <done>The progressive blueprint exists as a reusable component with a collapsed-only mode for chat reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Five review layers, walkthrough block, and approve/reject footer in NodeDetailPanel</name>
  <files>ui_service/src/components/pipeline/NodeDetailPanel.tsx, ui_service/src/app/pipeline/page.tsx</files>
  <read_first>
    - ui_service/src/components/pipeline/NodeDetailPanel.tsx (full, 313 lines — shell, typeConfig, section rhythm, pre/pass-fail/lock patterns)
    - ui_service/src/components/ui/error-states.tsx (full — `EmptyState` and `DegradedBanner` signatures to reuse verbatim)
    - ui_service/src/machines/pipelinePage.ts (plan 08-07 — reviewPanel states, APPROVE/REJECT/RETRY events, context fields)
    - ui_service/src/app/pipeline/page.tsx (after plan 08-08 — the `useMachine` wiring and how `SelectedNode` is built)
    - orchestrator/admin_api.py (draft-diff endpoint + review payload) and shared/modules/drafts.py (`get_diff`) — decide the section-2 data source
    - .planning/phases/08-co-evolution-approval/08-UI-SPEC.md (Component Contracts §2, §4 timeline, §7 walkthrough; Copywriting Contract for exact button and confirmation copy)
  </read_first>
  <action>
    Add a `module` entry to `typeConfig` (`Puzzle` icon, amber/orange accent, label "Module") so module nodes get a
    correct header.

    Extend the component props with `review?: ModuleReview | null; audit?: ModuleAuditAttempt[]; reviewState?: string;
    actionError?: string | null; onApprove?: () => void; onReject?: (feedback?: string) => void; onRetryReview?: () => void`.
    All optional so every existing call site keeps compiling.

    Inside the existing content area (`<div className="flex-1 overflow-auto p-4 space-y-4">`), render the review
    surface only when the node is a module AND `data.pendingApproval === true`, keeping the existing Status section above it:

    1. Walkthrough block (UI-SPEC §7, D-08) ABOVE the accordion: `rounded-lg border border-violet-500/30 bg-violet-500/5 p-3`,
       header row `Sparkles` icon (12px, `text-violet-400`) + `"How this module works"`
       (`text-xs font-semibold text-violet-300 uppercase tracking-wide`), body rendered with `react-markdown` +
       `remark-gfm` (already dependencies) at `text-sm text-zinc-300`. Do NOT enable raw-HTML rendering — the
       walkthrough is model output. When `review.walkthrough` is empty, omit the block entirely.
    2. `@radix-ui/react-accordion` with `type="multiple"` and section 1 open by default, five sections in this exact
       order and iconography: (1) Blueprint / `LayoutTemplate` → `<BlueprintCard blueprint={review.blueprint} .../>`;
       (2) Code Diff / `GitCompare` → unified diff in the existing `<pre>` style (`text-[11px] bg-zinc-950 font-mono`),
       falling back to an explicit "built fresh, no draft diff" affordance per the read_first decision;
       (3) Sandbox Validation Report / `FlaskConical` → `review.validation_results` rendered with the existing
       `CheckCircle2`/`XCircle` pass-fail rows; (4) Requested Credentials / `KeyRound` → `review.credentials`
       rendered with the existing `Lock`/`Unlock` iconography, showing `auth_type` and instructions but NEVER a
       secret value; (5) Repair History / `History` → a vertical timeline over `audit`, one row per attempt with an
       `attempt_number` circle badge (`bg-zinc-800`), a stage label coloured from `STAGE_COLORS`, a status icon
       (`CheckCircle2` / `XCircle` / spinning `Loader2`), and `failure_fingerprint` truncated to 8 chars in
       `text-[10px] font-mono text-zinc-500`, joined by a `border-l border-zinc-700` rule.
       Accordion triggers reuse the panel's existing header rhythm (`text-xs font-medium text-zinc-400 uppercase
       tracking-wide`) in a `flex items-center justify-between px-3 py-2.5` row with a `ChevronDown` that rotates
       180° via `transition-transform`.
    3. Sticky footer `border-t border-zinc-700 px-4 py-3` below the scroll area with two full-width buttons:
       **"Approve Module"** (`bg-green-500 hover:bg-green-600`, `Check` icon) calling `onApprove`, and
       **"Reject Module"** (`bg-red-500 hover:bg-red-600`, `X` icon). Reject opens an optional feedback
       `<textarea>` (`text-sm bg-zinc-950 border border-zinc-700 rounded-md p-2`, 2 rows, placeholder
       "Optional: describe what to change…"). With non-empty feedback, submit immediately and show the D-09
       non-destructive copy from the Copywriting Contract. With EMPTY feedback, require a second explicit click:
       the button transitions to a red "Confirm Reject" state showing the destructive-confirmation copy verbatim
       from the Copywriting Contract ("This permanently deletes the generated code, artifact bundle, and version
       history for this module. This cannot be undone…"). No modal dialog — inline confirmation only (Phase 6 precedent).
    4. States: when `reviewState` is `loading` show a skeleton/spinner; when it is `approving`/`rejecting` disable
       both buttons and show a spinner; when it is `error` render `DegradedBanner` with `actionError` and a Retry
       button calling `onRetryReview`. Never render a blank section on failure.

    In `ui_service/src/app/pipeline/page.tsx`, pass the new props from the machine:
    `review={state.context.review}`, `audit={state.context.audit}`,
    `reviewState={typeof state.value === 'object' ? (state.value as any).reviewPanel : undefined}` (or a
    `state.matches({ reviewPanel: 'loading' })`-style derivation — prefer explicit `matches` calls over casting),
    `actionError={state.context.actionError}`, `onApprove={() => send({ type: 'APPROVE' })}`,
    `onReject={(feedback) => send({ type: 'REJECT', feedback })}`, `onRetryReview={() => send({ type: 'RETRY' })}`.
  </action>
  <verify>
    <automated>cd ui_service && npx tsc --noEmit && npm run build && npx next lint --dir src/components/pipeline</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AccordionItem\|Accordion.Item" ui_service/src/components/pipeline/NodeDetailPanel.tsx` shows five sections (or five disclosure blocks under the rejected-gate substitution)
    - `grep -n "Approve Module" ui_service/src/components/pipeline/NodeDetailPanel.tsx` and `grep -n "Confirm Reject"` both return matches (exact Copywriting Contract strings)
    - `grep -c "dangerouslySetInnerHTML" ui_service/src/components/pipeline/NodeDetailPanel.tsx` returns 0
    - `grep -n "BlueprintCard" ui_service/src/components/pipeline/NodeDetailPanel.tsx` returns a match inside section 1
    - `grep -n "DegradedBanner" ui_service/src/components/pipeline/NodeDetailPanel.tsx` returns a match (explicit error state, no silent blank)
    - `grep -n "state.context.review" ui_service/src/app/pipeline/page.tsx` returns a match
    - `cd ui_service && npm run build` succeeds
  </acceptance_criteria>
  <done>An admin can read the walkthrough, blueprint, diff, sandbox report, credentials, and repair history, then approve or reject with feedback — REQ-014's UI half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → ui_service dependency tree | a new package-manager install enters the build |
| LLM-generated walkthrough / code / manifest strings → rendered DOM | untrusted model output displayed to an admin |
| admin click → approve/reject mutation | a security decision initiated from the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | `npm install @radix-ui/react-accordion` | mitigate | Blocking human-verify gate before install (Task 1); package is not in RESEARCH's legitimacy table, so it is treated as [ASSUMED] under the fallback policy and is never auto-approvable |
| T-08-36 | Tampering | model-authored walkthrough / diff rendered as markdown | mitigate | `react-markdown` without `rehype-raw`; zero `dangerouslySetInnerHTML` in the panel (acceptance criterion); diff rendered inside `<pre>` as text |
| T-08-37 | Information Disclosure | credential section leaking secret values | mitigate | The review payload contains only `requires_api_key`, `auth_type`, `api_key_instructions` and credential NAMES — the panel renders exactly those fields and never queries the credential store |
| T-08-38 | Elevation of Privilege | a non-admin operating the approve button | transfer | Server-side `require_permission(Permission.WRITE_CONFIG)` (D-17) is authoritative; a 403 surfaces via `DegradedBanner` rather than being pre-empted client-side |
| T-08-39 | Destruction of data | accidental terminal rejection purging artifacts | mitigate | Two-click inline confirmation with the destructive copy verbatim, and a feedback textarea offering the reversible repair path first (D-09) |
</threat_model>

<verification>
- `cd ui_service && npm run build` succeeds and `npx next lint --dir src/components/pipeline` reports no new errors
- Manual (services up): with a module manifest set to `validated`, clicking its node shows the walkthrough block, five sections with section 1 open, and the approve/reject footer; rejecting with empty feedback requires two clicks
</verification>

<success_criteria>
- D-05, D-06, D-15 and the display half of D-08 are delivered inside the existing panel shell (no new panel component)
- Destructive terminal rejection is confirmed twice with the exact contracted copy
- No raw-HTML rendering path exists for model output
</success_criteria>

<output>
Create `.planning/phases/08-co-evolution-approval/08-09-SUMMARY.md` when done.
</output>
