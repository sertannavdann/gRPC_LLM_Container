---
phase: 09-ecs-context-canvas
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - ui_service/src/ecs/dirty.ts
  - ui_service/src/ecs/world.ts
  - ui_service/src/ecs/verbalize.ts
  - ui_service/src/ecs/__tests__/dirty.test.ts
  - ui_service/src/ecs/__tests__/verbalize.test.ts
autonomous: true
requirements: [D9-01, D9-03, D9-06, D9-07, D9-14, VAL-W0-VERBALIZE, VAL-W0-DIRTY]
user_setup: []

must_haves:
  truths:
    - "One miniplex World instance backs the canvas; there is no second store"
    - "Any mutation to an entity notifies React through touch()/subscribeWorld()/getWorldVersion()"
    - "c_state text is byte-identical for the same entity set regardless of insertion order"
    - "A mirror rebuilt from delta contexts alone equals the true world at every invocation"
  artifacts:
    - path: "ui_service/src/ecs/world.ts"
      provides: "miniplex World + byId map + add/removeById/mutate choke points + touch reactivity bridge"
      exports: ["createPipelineWorld", "canvasWorld", "touch", "subscribeWorld", "getWorldVersion"]
      min_lines: 70
    - path: "ui_service/src/ecs/dirty.ts"
      provides: "Shared dirty/removedIds tracker with a delta version counter"
      exports: ["createDirtyTracker"]
    - path: "ui_service/src/ecs/verbalize.ts"
      provides: "canonicalize() + toCompactDsl() + toDelta() with the unchanged-omitted header"
      exports: ["canonicalize", "toCompactDsl", "toDelta"]
    - path: "ui_service/src/ecs/__tests__/verbalize.test.ts"
      provides: "Canonical text stability under churn (spike 005b claim)"
    - path: "ui_service/src/ecs/__tests__/dirty.test.ts"
      provides: "Delta mirror-reconstruction exactness (spike 006 claim)"
  key_links:
    - from: "ui_service/src/ecs/world.ts"
      to: "ui_service/src/ecs/dirty.ts"
      via: "single shared tracker instance passed into the world factory"
      pattern: "createDirtyTracker"
    - from: "ui_service/src/ecs/world.ts"
      to: "miniplex"
      via: "new World()"
      pattern: "from ['\"]miniplex['\"]"
---

<objective>
Build the ECS substrate core: the one world, the reactivity bridge, the choke-point dirty tracker,
and the canonical verbalizer — plus the two unit suites that prove the spike claims those pieces
exist to deliver.

Purpose: this is a PORT, not a design. Every file here has a byte-adjacent spike source that is
reference-quality per 09-RESEARCH.md. Deviating from the cited source is a regression, not a
refinement. The reactivity bridge in particular is mandatory boilerplate, not trimmable: neither
miniplex nor miniplex-react fires on component-VALUE mutation, only on add/remove.

Output: `src/ecs/world.ts`, `src/ecs/dirty.ts`, `src/ecs/verbalize.ts`, and two green unit suites.
</objective>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ecs-context-canvas/09-PATTERNS.md
@.claude/skills/spike-findings-grpc-llm/references/ecs-pipeline-canvas.md
@ui_service/src/ecs/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dirty tracker + miniplex world with the touch() reactivity bridge</name>
  <files>ui_service/src/ecs/dirty.ts, ui_service/src/ecs/world.ts</files>
  <read_first>
    - .planning/spikes/006-delta-context-streaming/run.mjs lines 24-57 (THE dirty-tracker port source — `add`/`remove`/`mutate` choke points, `views()`, `viewOf()`; the entire tracker is ~20 lines)
    - .planning/spikes/005b-world-snapshot-miniplex/world.mjs (the miniplex World adapter port source — `add`/`remove`/`views` and why the adapter layer exists at all)
    - .claude/skills/spike-findings-grpc-llm/references/ecs-pipeline-canvas.md lines 45-71 (the touch()/subscribeWorld()/getWorldVersion() bridge — port verbatim) and lines 184-195 (Constraints: the bridge is mandatory, not trimmable)
    - ui_service/src/ecs/types.ts (EntityRecord / CanvasView / Kind vocabulary from plan 01)
    - .planning/phases/09-ecs-context-canvas/09-RESEARCH.md Pitfall 1 (module-level singleton + Next.js App Router SSR footgun)
  </read_first>
  <action>
    Create `ui_service/src/ecs/dirty.ts` with `'use client'` on line 1. Export
    `createDirtyTracker()` returning `{ dirty: Set<string>, removedIds: Set<string>, version: number
    accessor, markDirty(id), markRemoved(id), clearSinceLastInvocation() }`. `markDirty` adds to
    `dirty` and deletes from `removedIds`; `markRemoved` deletes from `dirty` and adds to
    `removedIds`; `clearSinceLastInvocation()` clears both Sets and increments the delta version
    counter, returning the new version. Per CONTEXT.md's Claude's-Discretion note, the delta version
    counter lives HERE in the ECS module, not in XState context.

    Create `ui_service/src/ecs/world.ts` with `'use client'` on line 1. It has two parts:

    (a) Module-level reactivity bridge, ported verbatim from the skill blueprint: a module-scope
    `const listeners = new Set<() => void>()`, `let version = 0`, and exported `touch()` (increments
    version, calls every listener), `subscribeWorld(listener)` (adds, returns an unsubscribe closure),
    `getWorldVersion()`. This is module-global on purpose — `useSyncExternalStore` requires a stable
    subscribe/getSnapshot pair.

    (b) `createPipelineWorld()` factory returning `{ world, byId, tracker, add, removeById, mutate,
    views, viewOf, entityCount }`. Internals: `const world = new World<EntityRecord>()` from miniplex,
    `const byId = new Map<string, EntityRecord>()`, `const tracker = createDirtyTracker()`.
      - `add(rec: EntityRecord)` — `world.add({ ...rec, creds: [...rec.creds], flags: [...rec.flags] })`,
        set `byId`, `tracker.markDirty(rec.id)`, return the entity. Copy the arrays; never share
        references with the caller's record (spike 007 mediation.mjs line 64 does exactly this).
      - `removeById(id)` — look up in `byId`, no-op when absent, else `world.remove(e)`,
        `byId.delete(id)`, `tracker.markRemoved(id)`.
      - `mutate(id, patch: Partial<EntityRecord>)` — `Object.assign` onto the live entity and
        `tracker.markDirty(id)` (spike 006 run.mjs lines 45-50).
      - `views(): CanvasView[]` — `[...world.entities].map(...)` stripping `x`/`y` and copying
        `creds`/`flags`; `viewOf(id): CanvasView | null`.
    These three functions are the ONLY choke points: plan 04's `sync.ts` and `mediation.ts` must call
    them rather than `world.add`/`world.remove` directly, or dirty tracking silently loses events.

    Export a module-level singleton `export const canvasWorld = createPipelineWorld();` — the single
    world every browser consumer shares. Tests use the factory for isolation, so no reset hook is
    needed. Add a comment stating the known deferred gap verbatim from 09-RESEARCH.md Pitfall 4:
    ECS-only fields do not survive a remove/re-add cycle; CONTEXT.md defers a grace period for that
    to production telemetry — do not build one speculatively here.

    Do NOT add ECS "systems" for validation/highlighting/layout (locked decision D9-14, spike 004
    measured 3x code for zero benefit) — those stay as `resolveLifecycle`-style pure functions.
  </action>
  <verify>
    <automated>cd ui_service && npx tsc --noEmit && npx vitest run src/ecs --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 ui_service/src/ecs/world.ts` is exactly `'use client';` and the same for `dirty.ts`.
    - `grep -v '^\s*//' ui_service/src/ecs/world.ts | grep -c "export function touch"` returns 1; same for `subscribeWorld` and `getWorldVersion`.
    - `grep -v '^\s*//' ui_service/src/ecs/world.ts | grep -c "new World"` returns exactly 1 (one world, no dual store).
    - `grep -c "export const canvasWorld" ui_service/src/ecs/world.ts` returns 1.
    - `cd ui_service && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>One world, one dirty tracker, three choke points, and a working external-store bridge.</done>
</task>

<task type="auto">
  <name>Task 2: Canonical verbalizer — compact DSL and delta rendering</name>
  <files>ui_service/src/ecs/verbalize.ts</files>
  <read_first>
    - .planning/spikes/006-delta-context-streaming/verbalize.mjs (THE port source — `canonicalize()` lines 12-14, `toCompactDsl()` lines 44-65; byte-identical to 005b's copy)
    - .planning/spikes/006-delta-context-streaming/run.mjs lines 59-67 (`renderDeltaContext` — the delta variant with the "unchanged entities omitted" header)
    - ui_service/src/ecs/types.ts (the C_STATE DSL GRAMMAR doc comment — the authoritative field order/segments)
    - .planning/phases/09-ecs-context-canvas/09-02-SUMMARY.md if present (the Python port's emitted sample; the TS output must match it field-for-field)
  </read_first>
  <action>
    Create `ui_service/src/ecs/verbalize.ts` with `'use client'` on line 1.

    Export `canonicalize<T extends { id: string }>(views: T[]): T[]` returning a COPY sorted by
    logical string id (`[...views].sort(...)`) — never sort in place, and never rely on insertion
    order or miniplex's numeric entity ids, which are not stable across churn. This function is the
    reason the output is byte-stable and must be called by every serializer in the phase.

    Export `toCompactDsl(views: CanvasView[]): string` implementing the plan-01 grammar exactly:
    header line first, then groups keyed by `kind` iterated in `[...groups.keys()].sort()` order,
    each preceded by `[${kind}]`, each group's rows already canonicalized. Per-line segments in this
    order: `${id} "${label}" ${status}`, `/${lifecycle}` when non-null, ` detail=${detail}` when
    non-null, ` latency=${Math.round(latency)}` when non-null, ` creds:${creds.join(',')}` when
    non-empty, ` flags:${flags.join(',')}` when non-empty. Strip `"` from labels. The rounded-integer
    latency rule matters: plan 02's Python port rounds identically so the two c_state producers
    cannot diverge on float formatting.

    Export `toDelta(dirty: Set<string>, removedIds: Set<string>, viewOf: (id: string) => CanvasView | null,
    version: number): { text: string; changed: CanvasView[]; removed: string[] }` porting run.mjs
    lines 60-67: first line
    `# pipeline_state DELTA since v${version} — unchanged entities omitted`; then, when `removedIds`
    is non-empty, `removed: ${[...removedIds].sort().join(',')}`; then, when there are changed views,
    `toCompactDsl(changed)` with its OWN header line dropped (`.split('\n').slice(1).join('\n')`);
    when neither, the literal `(no changes)`. Keep the header wording — CONTEXT.md flags the
    prompt-framing risk explicitly (a model may otherwise treat a delta as the full world).

    Do not add a JSON dump format. CONTEXT.md's locked decision forbids raw JSON in LLM context
    (measured 2.9x token cost); `history.ts` in plan 05 uses `JSON.stringify(canonicalize(...))` for
    SNAPSHOTS only, which is a different consumer.
  </action>
  <verify>
    <automated>cd ui_service && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' ui_service/src/ecs/verbalize.ts | grep -c "export function canonicalize"` returns 1; same for `toCompactDsl` and `toDelta`.
    - `grep -c "unchanged entities omitted" ui_service/src/ecs/verbalize.ts` returns 1.
    - `grep -c "pipeline_state — line format" ui_service/src/ecs/verbalize.ts` returns 1.
    - `toCompactDsl([])` returns exactly the header line.
    - `cd ui_service && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>c_state and delta text generation exists in TypeScript, matching the Python port's grammar.</done>
</task>

<task type="auto">
  <name>Task 3: Unit suites — canonical stability and delta mirror-reconstruction</name>
  <files>ui_service/src/ecs/__tests__/verbalize.test.ts, ui_service/src/ecs/__tests__/dirty.test.ts</files>
  <read_first>
    - .planning/spikes/006-delta-context-streaming/run.mjs lines 69-135 (THE test-logic port source — `applyFullToMirror`/`applyDeltaToMirror`/`mirrorEquals`, the 40-tick churn session, and the "mirror correct at every invocation" assertion)
    - .planning/spikes/005b-world-snapshot-miniplex/run.mjs (canonical-ordering / format-stability assertion style)
    - .planning/spikes/007-llm-writes-world/fixture.mjs (the 26-entity deterministic fixture shape to adapt — 7 services, 15 modules, 4 stages)
    - ui_service/src/ecs/world.ts and ui_service/src/ecs/verbalize.ts (as written in Tasks 1-2)
    - .planning/phases/09-ecs-context-canvas/09-VALIDATION.md (the two Wave 0 rows these files close)
  </read_first>
  <action>
    Both files import `describe`/`it`/`expect` from `vitest` explicitly (plan 01 set `globals: false`).
    Both build fresh worlds via `createPipelineWorld()` — never the `canvasWorld` singleton — so
    tests are order-independent.

    `verbalize.test.ts` (closes the spike 005b "canonical text stable across churn" claim):
      - Build a deterministic fixture of ~20 entities spanning all five kinds, adapted from
        `.planning/spikes/007-llm-writes-world/fixture.mjs` to the plan-01 `EntityRecord` shape.
      - Test "byte-stable across insertion-order permutations": add the fixture to world A in order
        and to world B in reversed order; `expect(toCompactDsl(A.views())).toBe(toCompactDsl(B.views()))`.
      - Test "byte-stable across remove/re-add churn": from world A, remove three entities and re-add
        the same records; output must equal the pre-churn output exactly.
      - Test "grouping and ordering": assert group headers appear in ascending kind order and that
        ids within a group are ascending.
      - Test "optional segments omitted": an entity with `lifecycle: null, detail: null, latency: null,
        creds: [], flags: []` renders a line with exactly three space-separated segments and no
        `detail=`/`latency=`/`creds:`/`flags:` tokens.
      - Test "drag does not change c_state": mutate an entity's `x`/`y` and assert `toCompactDsl`
        output is unchanged (this is why `CanvasView` excludes position).

    `dirty.test.ts` (closes the spike 006 "delta mirror-reconstruction exact" claim — a direct port
    of run.mjs lines 74-135):
      - Implement the mirror harness: `mirror = new Map<string, CanvasView>()`,
        `applyFullToMirror(views)`, `applyDeltaToMirror(delta)` (delete `delta.removed`, set
        `delta.changed`), and `mirrorEquals(views)` comparing
        `JSON.stringify(canonicalize(views))` against `JSON.stringify(canonicalize([...mirror.values()]))`.
      - Drive a deterministic 40-tick churn script (seeded/hardcoded array of `metrics`/`status`/
        `remove`/`add` ops — port the shape from `.planning/spikes/006-delta-context-streaming/churn.mjs`
        or inline a fixed script; do NOT use `Math.random()`), invoking a delta every 5 ticks via
        `toDelta(tracker.dirty, tracker.removedIds, viewOf, version)` followed by
        `tracker.clearSinceLastInvocation()`.
      - Assert `mirrorEquals(world.views())` is true at EVERY invocation (the single most important
        assertion in this suite) and that at least one invocation had a non-empty `removed` list and
        one had a non-empty `changed` list, so the test cannot pass vacuously.
      - Assert `markDirty` then `markRemoved` on the same id leaves the id in `removedIds` only, and
        `markRemoved` then `markDirty` leaves it in `dirty` only (the Set-invariant the tracker's
        correctness rests on).
  </action>
  <verify>
    <automated>cd ui_service && npx vitest run src/ecs/__tests__/verbalize.test.ts src/ecs/__tests__/dirty.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui_service && npx vitest run src/ecs/__tests__/verbalize.test.ts src/ecs/__tests__/dirty.test.ts` exits 0.
    - `verbalize.test.ts` contains at least 5 `it(` blocks including one asserting `.toBe()` byte-equality across reordered insertion.
    - `dirty.test.ts` asserts mirror equality inside a loop over invocations, not once.
    - Neither file imports `canvasWorld` (singleton) — `grep -c "canvasWorld" ui_service/src/ecs/__tests__/*.test.ts` returns 0.
    - Combined runtime under 5 seconds.
  </acceptance_criteria>
  <done>Two of the five phase Done Criteria (c_state byte-stability, delta mirror reconstruction) have automated proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE payload (backend-computed) -> ECS world | Data crossing into browser state; treated as authoritative for entity existence |
| ECS world -> LLM context text | Entity fields become model-visible text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information Disclosure | `verbalize.ts` c_state text | mitigate | `CanvasView` (plan 01) carries credential NAMES at most, never values; the backend port emits `creds: []`. Asserted indirectly by plan 02's `test_no_credential_material_emitted`; do not add a field that reads from a credential store |
| T-09-09 | Tampering | Bypassing `add`/`removeById`/`mutate` and calling `world.add`/`world.remove` directly | mitigate | Choke-point contract documented at the top of `world.ts`; plans 04/05 grep-gated on not calling `world.add` outside `world.ts`. Bypass silently corrupts dirty tracking, deltas, and replay determinism |
| T-09-10 | Denial of Service | Unbounded `dirty`/`removedIds` growth if `clearSinceLastInvocation()` is never called | accept | Sets are bounded by entity count (dozens); the consumer (plan 09/10) clears per invocation |
| T-09-SC | Tampering | miniplex import | mitigate | Version pinned to the slopcheck-approved 2.0.0 in plan 01; no new package installs in this plan |
</threat_model>

<verification>
- `cd ui_service && npm run test` exits 0 (types + verbalize + dirty suites).
- `cd ui_service && npx tsc --noEmit` exits 0.
- `grep -rn "new World" ui_service/src --include=*.ts | wc -l` returns 1 — the single-world invariant holds.
</verification>

<success_criteria>
- One world, one dirty tracker, one verbalizer; no parallel/dual store anywhere in `src/ecs/`.
- c_state byte-stability and delta mirror-reconstruction are both proven by `vitest run` in under 5 seconds.
- Every file under `src/ecs/` carries its own `'use client'` directive (Pitfall 1).
</success_criteria>

<output>
Create `.planning/phases/09-ecs-context-canvas/09-03-SUMMARY.md` when done. Include the exported
signatures of `world.ts`/`verbalize.ts` so plans 04-10 do not need to re-read the source.
</output>
</content>
