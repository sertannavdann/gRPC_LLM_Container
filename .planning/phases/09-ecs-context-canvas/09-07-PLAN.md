---
phase: 09-ecs-context-canvas
plan: 07
type: execute
wave: 4
depends_on: ["09-04", "09-06"]
files_modified:
  - ui_service/src/app/pipeline/canvasGraph.ts
  - ui_service/src/app/pipeline/__tests__/canvasGraph.test.ts
  - ui_service/src/app/pipeline/page.tsx
  - ui_service/e2e/pipeline-ecs-churn.spec.ts
autonomous: true
requirements: [D9-02, D9-03, D9-04, VAL-W0-CHURN-E2E]
user_setup: []

must_haves:
  truths:
    - "The pipeline canvas renders from the ECS world and stays rendered under sustained SSE churn"
    - "React Flow's nodes array is never replaced wholesale on an ECS touch() — it is patched by id"
    - "Dragging a node writes its position back to ECS on drag-stop only, never mid-drag"
    - "A dragged node keeps its new position across the next SSE tick, proven in a real browser"
    - "Hovering a node leaves the canvas stable under churn with no page errors"
    - "Selecting a module that a later SSE snapshot omits closes the detail panel in the real page"
  artifacts:
    - path: "ui_service/src/app/pipeline/canvasGraph.ts"
      provides: "Pure buildCanvasGraph(pipeline, positions) — the layout math extracted from page.tsx"
      exports: ["buildCanvasGraph", "STAGE_NODES", "STAGE_EDGES"]
      min_lines: 120
    - path: "ui_service/src/app/pipeline/page.tsx"
      provides: "Buffered React Flow integration: sync effect + version patch effect + drag-stop write-back"
      contains: "useSyncExternalStore"
    - path: "ui_service/e2e/pipeline-ecs-churn.spec.ts"
      provides: "Playwright proof of churn stability, real drag with position persistence, hover stability, and DOM-level stale-selection clearing"
  key_links:
    - from: "ui_service/src/app/pipeline/page.tsx"
      to: "ui_service/src/ecs/world.ts"
      via: "useSyncExternalStore(subscribeWorld, getWorldVersion)"
      pattern: "useSyncExternalStore\\(subscribeWorld"
    - from: "ui_service/src/app/pipeline/page.tsx"
      to: "ui_service/src/ecs/sync.ts"
      via: "useEffect keyed on state.context.pipeline"
      pattern: "syncFromPipeline"
---

<objective>
Re-plumb the pipeline canvas onto the ECS world using the buffered React Flow pattern, and prove it
survives real SSE churn with a Playwright spec.

Purpose: this is the highest-risk change in the phase. Spike 001 confirmed that deriving React
Flow's `nodes` prop fresh from `world.entities.map()` on every `touch()` does not degrade rendering
— it BREAKS it outright (blank canvas, ~2700-4900 renders/node/sec, broken even at rest). The
buffered pattern (React Flow owns `nodes` via `useNodesState`; ECS patches non-positional fields by
id; position writes back on drag-stop only) is mandatory and non-negotiable.

Output: extracted pure layout builder + its unit test, the rewired page, and the churn E2E spec.
</objective>

<execution_context>
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sertanavdan/Documents/Software/AI/gRPC_llm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ecs-context-canvas/09-PATTERNS.md
@.claude/skills/spike-findings-grpc-llm/references/ecs-pipeline-canvas.md
@.planning/phases/09-ecs-context-canvas/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the layout math into a pure buildCanvasGraph() with a unit test</name>
  <files>ui_service/src/app/pipeline/canvasGraph.ts, ui_service/src/app/pipeline/__tests__/canvasGraph.test.ts</files>
  <read_first>
    - ui_service/src/app/pipeline/page.tsx lines 39-311 (the code being extracted: `STAGE_NODES`, `STAGE_EDGES`, `stageToToolEdge`, `stateEdgeColor`, `toolToAdapterEdge`, and the whole rebuild `useEffect` body — services row, tools-by-stage row, adapters row with position dedup, modules row with pending-first sort)
    - ui_service/src/machines/pipelinePage.ts (plan 06 added `STAGE_NODE_IDS` — import it, do NOT redefine the four stage ids)
    - ui_service/src/components/pipeline/ModuleNode.tsx lines 23-33 (`ModuleNodeData` — the node `data` shape this builder must keep producing verbatim, per the UI-SPEC visual regression lock)
    - .planning/phases/09-ecs-context-canvas/09-UI-SPEC.md ("Visual Regression Lock" — `page.tsx`'s node/edge color conventions, `stateEdgeColor`, `STAGE_COLORS` are unchanged by this phase)
  </read_first>
  <action>
    Create `ui_service/src/app/pipeline/canvasGraph.ts` with `'use client'` on line 1. Move — do not
    reimagine — the layout logic currently inline in `page.tsx` lines 49-311:
    `STAGE_NODES`, `STAGE_EDGES`, `stageToToolEdge`, `stateEdgeColor`, `toolToAdapterEdge`, and the
    node/edge construction for services (y=-80), tools grouped by stage (y=230), adapters with the
    `adapterPositions` dedup map (y=420), and modules with the pending-approval-first sort (y=600).
    The math, the ids, the `data` payloads, and the edge styles must be byte-equivalent to today's
    behavior — this is an extraction, and the UI-SPEC locks "no visual regression" as binding.

    Export `buildCanvasGraph(pipeline: PipelineState | null, positions?: Map<string, {x:number;y:number}>):
    { nodes: Node[]; edges: Edge[]; layout: Map<string, {x:number;y:number}> }`:
      - `layout` is the computed default position per node id — plan 04's `toEntityRecords(pipeline,
        layout)` consumes it to seed new ECS entities.
      - When `positions` is supplied and contains an id, that position WINS over the computed layout.
        This is how a user's dragged position (held in the ECS entity's `x`/`y`) survives a topology
        rebuild.
      - With `pipeline === null`, return just the static stage nodes/edges.
    Import `STAGE_NODE_IDS` from `@/machines/pipelinePage` and assert (in a comment, and by using it)
    that `STAGE_NODES` ids match it.

    Create `ui_service/src/app/pipeline/__tests__/canvasGraph.test.ts` (vitest, explicit imports):
      - null pipeline yields exactly the four stage nodes and three stage edges;
      - a fixture pipeline yields the expected node ids (`svc-*`, `tool-*`, `adapter-*`, `module-*`)
        with no duplicate ids;
      - pending-approval modules sort leftmost (lower `x` than non-pending ones);
      - a supplied `positions` entry overrides the computed layout for that id while others keep
        their computed values;
      - `layout` contains an entry for every returned node id.
  </action>
  <verify>
    <automated>cd ui_service && npx vitest run src/app/pipeline/__tests__/canvasGraph.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui_service && npx vitest run src/app/pipeline/__tests__/canvasGraph.test.ts` exits 0 with 5 or more `it(` blocks.
    - `grep -c "export function buildCanvasGraph" ui_service/src/app/pipeline/canvasGraph.ts` returns 1.
    - `grep -c "STAGE_NODE_IDS" ui_service/src/app/pipeline/canvasGraph.ts` returns 1 or more (imported, not redefined).
    - `grep -v '^\s*//' ui_service/src/app/pipeline/canvasGraph.ts | grep -c "useState\|useEffect"` returns 0 (the builder is pure).
    - `cd ui_service && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The layout math is a pure, unit-tested function the page can call from either effect.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire page.tsx onto the buffered React Flow pattern</name>
  <files>ui_service/src/app/pipeline/page.tsx</files>
  <read_first>
    - ui_service/src/app/pipeline/page.tsx (the file being modified — especially lines 107-137 `useNodesState`/`onNodeClick`/`selectedNode`, the rebuild effect at 140-311 being REPLACED, and line 370 where the RF-generated `onNodesChange` is passed straight through)
    - .claude/skills/spike-findings-grpc-llm/references/ecs-pipeline-canvas.md lines 77-107 (THE target shape: the patch effect keyed on `version`, and the `onNodesChange` drag-stop write-back with the `draggingId` ref)
    - .claude/skills/spike-findings-grpc-llm/references/ecs-pipeline-canvas.md lines 152-183 (What to Avoid — the fresh-array anti-pattern and the "don't tag every entity on any change event" bug)
    - .planning/phases/09-ecs-context-canvas/09-RESEARCH.md Pitfall 2 (StrictMode double-invoke; `next.config.js` has `reactStrictMode: true`) and Pitfall 3 (mid-drag removal, unverified)
    - ui_service/src/ecs/sync.ts, ui_service/src/ecs/world.ts (as built in plans 03-04)
  </read_first>
  <action>
    Replace the single rebuild `useEffect` (lines 140-311) with the buffered three-part wiring. Keep
    everything else in the file untouched — the toolbar, `nodeTypes`, `onNodeClick` -> `SELECT_NODE`,
    `NodeDetailPanel` props, `MiniMap`/`Controls`/`Background` chrome are all locked by the UI-SPEC
    visual regression section.

    (a) Subscribe to the ECS: `const version = useSyncExternalStore(subscribeWorld, getWorldVersion,
    getWorldVersion);` — pass `getWorldVersion` as the third (server snapshot) argument so Next.js
    App Router SSR/hydration does not throw.

    (b) SSE ingestion effect, keyed on `[pipeline]` (i.e. `state.context.pipeline`, whose reference
    XState replaces only on genuine new data — NOT a raw `useEffect([])`, per Pitfall 2 StrictMode
    double-invocation): when `pipeline` is non-null, compute
    `const { nodes: builtNodes, edges: builtEdges, layout } = buildCanvasGraph(pipeline, currentEcsPositions())`
    where `currentEcsPositions()` reads `x`/`y` from the live ECS entities; then call
    `syncFromPipeline(pipeline, layout)`; then compare the built node-id set against the current
    `nodes` id set and call `setNodes(builtNodes)`/`setEdges(builtEdges)` ONLY when the id set (the
    topology) actually changed. A wholesale replace on every 2s tick is exactly what the buffered
    pattern exists to stop.

    (c) Patch effect, keyed on `[version]`: `setNodes(prev => prev.map(n => {...}))` — look the entity
    up by `n.id` via the ECS `byId`; return `n` unchanged when there is no entity OR when every
    mirrored field the node renders is already equal (`label`, `status`/`state`, `lifecycle`,
    `detail`/`buildStage`, `flags` including `pending_approval`); otherwise return a NEW node object
    with a NEW `data` object carrying only the changed mirrored fields. Never build the array from
    `world.entities.map()`; never call `setNodes(freshArray)` here. Returning the identical `n`
    reference for unchanged nodes is what preserves React Flow's internal measurement tracking.

    (d) Drag-stop write-back: define `const draggingId = useRef<string | null>(null)` and a
    `onNodesChange` callback that iterates the `changes` array and, for `c.type === 'position'`,
    sets `draggingId.current = c.id` while `c.dragging` is true and, on drag-stop, looks up
    `nodes.find(n => n.id === c.id)` and the ECS entity by id and writes `entity.x = n.position.x;
    entity.y = n.position.y` — ONLY for the entity that actually moved (spike bug: looping the whole
    array and tagging every entity). Guard with `if (n && entity)`; per Pitfall 3 this guard is the
    full extent of spike-tested protection against a node removed mid-drag, so add a comment marking
    it as an unverified edge case slated for the plan-10 manual check. Finish by delegating to the
    React Flow-generated handler (rename the `useNodesState` third element to `onNodesChangeRF`).

    Do NOT call `touch()` from the page — mutations funnel through the ECS module's choke points,
    which already notify.
  </action>
  <verify>
    <automated>cd ui_service && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useSyncExternalStore(subscribeWorld" ui_service/src/app/pipeline/page.tsx` returns 1.
    - `grep -c "syncFromPipeline" ui_service/src/app/pipeline/page.tsx` returns 1.
    - `grep -c "world.entities" ui_service/src/app/pipeline/page.tsx` returns 0 (no fresh-array derivation anywhere in the page).
    - The `[version]`-keyed effect contains `setNodes((prev) => prev.map(` and does NOT contain a bare `setNodes(` with an array literal or a mapped world collection.
    - `grep -c "draggingId" ui_service/src/app/pipeline/page.tsx` returns 3 or more (ref declaration + dragging branch + drag-stop branch).
    - `cd ui_service && npm run build` exits 0.
    - `git diff ui_service/src/app/pipeline/page.tsx` shows NO changes to the toolbar JSX, `nodeTypes`, `onNodeClick`, or the `NodeDetailPanel` element (visual regression lock).
  </acceptance_criteria>
  <done>The canvas renders from the ECS world through a controlled buffer, with position write-back on drag-stop only.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright spec — churn, real drag, hover, and DOM-level stale selection</name>
  <files>ui_service/e2e/pipeline-ecs-churn.spec.ts</files>
  <read_first>
    - ui_service/e2e/pipeline-sse.spec.ts (THE structural analog, full file — header comment convention, `import { DASHBOARD_COMPOSE_SERVICE } from './util'`, the `ALLOW_RESTART` gate, the always-restore `finally` block, and the "assert BOTH the toolbar text AND the canvas DOM element" convention)
    - ui_service/e2e/util.ts (`E2E_BASE_URL`, `E2E_DASHBOARD_URL`, `DASHBOARD_COMPOSE_SERVICE`, `PROBE_TIMEOUT_MS`)
    - ui_service/playwright.config.ts (runs against the live docker-compose stack; there is no dev-server launcher)
    - .planning/phases/09-ecs-context-canvas/09-PATTERNS.md ("pipeline-ecs-churn.spec.ts" section — including the non-mocking constraint: Playwright request interception does not reliably deliver events to `EventSource`)
    - dashboard_service/pipeline_stream.py lines 192-233 and 268-298 (`_build_pending_approval_list` scans MODULES_DIR on EVERY 2s cycle and appends manifest-only modules to the snapshot — this is the non-disruptive lever Test 4 uses to make an entity genuinely appear and disappear)
    - modules/test/hello/manifest.json (the manifest field shape to copy for the throwaway probe module)
    - ui_service/src/machines/pipelinePage.ts (plan 06's `clearSelectionIfMissing` — the behavior Test 4 proves in the real page rather than in isolation)
  </read_first>
  <action>
    Create `ui_service/e2e/pipeline-ecs-churn.spec.ts` following `pipeline-sse.spec.ts`'s structure
    exactly: the same header-comment style stating what is under test and why the real SSE endpoint is
    used, the same imports, the same `ALLOW_RESTART` gating for anything disruptive. Register a
    `page.on('pageerror', ...)` collector in a `beforeEach` and assert it stayed empty at the end of
    every test — an uncaught React error under churn must fail the suite, not pass silently.

    TEST 1 — `canvas survives sustained SSE churn without going blank` (non-disruptive, always runs):
    `page.goto('/pipeline')`, assert `getByText('Live')` and `.react-flow` are visible, then let the
    real 2s stream run for at least 5 ticks (~12s) by re-asserting inside a loop rather than one sleep
    followed by a single assertion. After the churn window assert `.react-flow` is still visible, at
    least one `.react-flow__node` is visible, the toolbar still reads `Live`, and the node COUNT is
    non-zero and identical across the last two polls — a blank canvas and a thrashing canvas are
    different failures and both must be caught.

    TEST 2 — `dragged node keeps its position across the next SSE tick` (the drag half of the
    "drag/hover verified via Playwright" Done Criterion). Pick a stable node
    (`page.locator('.react-flow__node').first()`), read its `boundingBox()`, and perform a REAL drag
    with `page.mouse.move(startX, startY)`, `page.mouse.down()`, two or three intermediate
    `page.mouse.move(...)` steps of ~120px total offset (React Flow needs more than one move event to
    register a drag), then `page.mouse.up()`. Assert during the drag that `.react-flow` is still
    visible (no blank canvas mid-gesture). After `mouse.up()`, read the new `boundingBox()` and assert
    it moved by approximately the applied offset (tolerance ±10px). Then wait for at least TWO SSE
    ticks (~5s) and re-read the bounding box: it must still be at the moved position (same ±10px
    tolerance), proving the drag-stop write-back into ECS survived the next snapshot's topology pass
    instead of snapping back to the computed layout. Use the node's `data-id` attribute to re-locate
    the same node after the ticks rather than relying on `.first()` ordering.

    TEST 3 — `hovering a node leaves the canvas stable under churn` (the hover half). Hover a node
    with `page.hover()` on its `data-id` selector, hold the hover across two SSE ticks by re-asserting
    in a loop, and assert: the hovered node is still visible, its bounding box is unchanged, the total
    `.react-flow__node` count is unchanged, and the `pageerror` collector is still empty. Note in a
    comment that 09-UI-SPEC.md's Interaction Contracts LOCK hover as visually unchanged this phase, so
    this test asserts stability under hover rather than a new hover treatment — do not invent a hover
    visual to assert against.

    TEST 4 — `selection clears in the DOM when the selected module disappears from a snapshot`
    (non-disruptive; the production-page half of the stale-selection Done Criterion that plan 06 proves
    at the machine level). Drive it through the dashboard's manifest scan, which re-reads MODULES_DIR
    every 2s with no restart and no auth:
      1. In a `beforeAll`/setup step, write `modules/e2e_probe/probe/manifest.json` on the host
         (resolve the repo root from `import.meta.url`/`process.cwd()`; the same directory is bind
         mounted into the dashboard at `/app/modules`), copying the field shape from
         `modules/test/hello/manifest.json` with `name: 'probe'`, `category: 'e2e_probe'`,
         `platform: 'probe'`, `display_name: 'E2E Probe'`, `status: 'pending'`.
      2. `page.goto('/pipeline')` and wait (up to 3 ticks) for the node
         `[data-id="module-e2e_probe/probe"]` to appear.
      3. Click it and assert the `NodeDetailPanel` is open — assert on a panel-scoped element showing
         the probe's label, not merely on any panel-ish container.
      4. Delete `modules/e2e_probe` from the host filesystem (`fs.rmSync(..., { recursive: true,
         force: true })`).
      5. Assert, within 3 ticks, that the node `[data-id="module-e2e_probe/probe"]` is gone AND the
         detail panel showing its label is gone — this is `clearSelectionIfMissing` firing in the real
         page. Per 09-UI-SPEC.md's Interaction Contracts the clear is SILENT: assert no toast and no
         error banner appeared.
      6. Always remove `modules/e2e_probe` in a `finally`/`afterAll` so a failed assertion cannot leave
         a stray module in the developer's stack.

    TEST 5 — gated behind `E2E_ALLOW_RESTART`, `canvas recovers from a dashboard restart without a
    blank canvas`: restart the dashboard compose service inside a `try`, assert `Disconnected` then
    `Live` return, assert `.react-flow` stayed visible throughout, and restore the service in
    `finally` exactly like `pipeline-sse.spec.ts` does.

    Do NOT mock or intercept the SSE endpoint — `pipeline-sse.spec.ts`'s own header documents that
    Playwright's request-interception API does not reliably deliver events to `EventSource`.
  </action>
  <verify>
    <automated>cd ui_service && npx playwright test e2e/pipeline-ecs-churn.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui_service && npx playwright test e2e/pipeline-ecs-churn.spec.ts` exits 0 with the docker-compose stack running (`make up`), running 4 tests by default (Test 5 skipped without `E2E_ALLOW_RESTART`).
    - `grep -c "page.mouse.down" ui_service/e2e/pipeline-ecs-churn.spec.ts` returns 1 or more and `grep -c "page.mouse.up"` returns 1 or more (a real drag gesture, not a synthetic event).
    - `grep -c "page.hover\|\.hover(" ui_service/e2e/pipeline-ecs-churn.spec.ts` returns 1 or more.
    - `grep -c "e2e_probe" ui_service/e2e/pipeline-ecs-churn.spec.ts` returns 4 or more (create, wait, delete, cleanup).
    - `grep -c "pageerror" ui_service/e2e/pipeline-ecs-churn.spec.ts` returns 1 or more.
    - `grep -c "page.route\|context.route" ui_service/e2e/pipeline-ecs-churn.spec.ts` returns 0 (no SSE mocking).
    - Test 2 asserts the post-drag bounding box BOTH immediately after `mouse.up()` and again after two SSE ticks.
    - Test 4 asserts the detail panel disappeared, not only that the node disappeared.
    - After a full run, `ls modules/e2e_probe` reports "No such file or directory" (cleanup ran).
    - Non-disruptive tests complete in under 120 seconds total.
  </acceptance_criteria>
  <done>Churn stability, drag-with-position-persistence, hover stability, and stale-selection clearing are all proven in a real browser against the real SSE stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream -> React Flow render tree | Backend-computed data drives DOM node identity and count |
| user drag gesture -> ECS entity position | The only user-owned write into the ECS world |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Denial of Service (self-inflicted) | Fresh-array derivation of `nodes` from the ECS on every `touch()` | mitigate | Buffered patch-by-id effect; grep-gated (`world.entities` count is 0 in page.tsx); spike 001 measured this as a hard rendering break, not a slowdown |
| T-09-17 | Tampering | Drag-stop write-back to an entity removed mid-drag by an intervening SSE sync | accept (documented) | `if (n && entity)` existence guard is the full extent of spike-tested protection (09-RESEARCH.md A4/Pitfall 3, explicitly unverified); flagged in code and slated for the plan-10 manual verification rather than speculative machinery |
| T-09-18 | Tampering | Every entity tagged as user-touched because `onNodesChange` loops the whole array | mitigate | Only the `NodeChange` entries with `type === 'position'` and a matching id are written back — the exact spike bug called out in the skill blueprint's What-to-Avoid |
| T-09-19 | Denial of Service | StrictMode double-invocation re-running the SSE ingestion effect | mitigate | Effect is keyed on `state.context.pipeline` reference identity (XState replaces it only on genuine new data); `syncFromPipeline` is idempotent for identical records; history appends are NOT wired here (plan 10 owns that and its manual StrictMode check) |
</threat_model>

<verification>
- `cd ui_service && npm run test` exits 0 (all unit suites, including the new canvasGraph suite).
- `cd ui_service && npm run build` exits 0.
- With the stack up: `cd ui_service && npx playwright test e2e/pipeline-sse.spec.ts e2e/pipeline-ecs-churn.spec.ts` exits 0 — the pre-existing SSE suite must not regress.
</verification>

<success_criteria>
- The canvas renders from the ECS world with no full-array re-derivation anywhere.
- Sustained real SSE churn leaves the canvas rendered with a stable, non-zero node count.
- Dragged positions survive an SSE topology change, verified by a real Playwright drag gesture.
- Hovering under churn produces no page errors and no canvas instability.
- A module that vanishes from a snapshot closes its detail panel in the real page (production-page
  counterpart to plan 06's machine-level proof).
</success_criteria>

<output>
Create `.planning/phases/09-ecs-context-canvas/09-07-SUMMARY.md` when done. Record the final shape of
the three effects (ingestion / patch / drag-stop) so plans 08-10 extend rather than re-derive them.
</output>
</content>
